<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CentOS下安装OracleJDK</title>
    <url>/2019/09/29/CentOS%E4%B8%8B%E5%AE%89%E8%A3%85OracleJDK/</url>
    <content><![CDATA[<h3 id="获取OracleJDK"><a href="#获取OracleJDK" class="headerlink" title="获取OracleJDK"></a>获取OracleJDK</h3><blockquote>
<p>以<strong>Java SE Development Kit 8u221</strong>为例，下载地址如下：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https:&#x2F;&#x2F;www.oracle.com&#x2F;technetwork&#x2F;java&#x2F;javase&#x2F;downloads&#x2F;jdk8-downloads-2133151.html</span><br></pre></td></tr></table></figure>
<h3 id="解压到目录"><a href="#解压到目录" class="headerlink" title="解压到目录"></a>解压到目录</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tar zxvf jdk-8u151-linux-x64.tar.gz -C &#x2F;usr&#x2F;jdk</span><br></pre></td></tr></table></figure>
<h3 id="配置环境变量"><a href="#配置环境变量" class="headerlink" title="配置环境变量"></a>配置环境变量</h3><blockquote>
<p>编辑**/etc/profile**文件，在最后添加如下内容：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export JAVA_HOME&#x3D;&#x2F;usr&#x2F;jdk&#x2F;jdk1.8.0_151</span><br><span class="line">export JRE_HOME&#x3D;$&#123;JAVA_HOME&#125;&#x2F;jre</span><br><span class="line">export CLASSPATH&#x3D;.:$&#123;JAVA_HOME&#125;&#x2F;lib:$&#123;JRE_HOME&#125;&#x2F;lib</span><br><span class="line">export PATH&#x3D;$&#123;JAVA_HOME&#125;&#x2F;bin:$PATH</span><br></pre></td></tr></table></figure>
<h3 id="验证结果"><a href="#验证结果" class="headerlink" title="验证结果"></a>验证结果</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># java -version</span><br><span class="line">java version &quot;1.8.0_151&quot;</span><br><span class="line">Java(TM) SE Runtime Environment (build 1.8.0_151-b12)</span><br><span class="line">Java HotSpot(TM) 64-Bit Server VM (build 25.151-b12, mixed mode)</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS下安装Docker记录</title>
    <url>/2019/05/07/CentOS%E4%B8%8B%E5%AE%89%E8%A3%85Docker%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="安装包的方式"><a href="#安装包的方式" class="headerlink" title="安装包的方式"></a>安装包的方式</h3><ol>
<li><p>下载安装包</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">wget https:&#x2F;&#x2F;download.docker.com&#x2F;linux&#x2F;centos&#x2F;7&#x2F;x86_64&#x2F;stable&#x2F;Packages&#x2F;docker-ce-18.06.1.ce-3.el7.x86_64.rpm</span><br></pre></td></tr></table></figure></li>
<li><p>安装docker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install docker-ce-18.06.1.ce-3.el7.x86_64.rpm</span><br></pre></td></tr></table></figure></li>
<li><p>启动docker</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">systemctl start docker</span><br></pre></td></tr></table></figure></li>
<li><p>开机自动启动</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">systemctl enable docker</span><br></pre></td></tr></table></figure></li>
<li><p>验证docker是否安装成功</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run hello-world</span><br><span class="line"></span><br><span class="line">Hello from Docker!</span><br><span class="line">This message shows that your installation appears to be working correctly.</span><br><span class="line">To generate this message, Docker took the following steps:</span><br><span class="line"> 1. The Docker client contacted the Docker daemon.</span><br><span class="line"> 2. The Docker daemon pulled the &quot;hello-world&quot; image from the Docker Hub.</span><br><span class="line">    (amd64)</span><br><span class="line"> 3. The Docker daemon created a new container from that image which runs the</span><br><span class="line">    executable that produces the output you are currently reading.</span><br><span class="line"> 4. The Docker daemon streamed that output to the Docker client, which sent it</span><br><span class="line">    to your terminal.</span><br><span class="line">To try something more ambitious, you can run an Ubuntu container with:</span><br><span class="line"> $ docker run -it ubuntu bash</span><br><span class="line">Share images, automate workflows, and more with a free Docker ID:</span><br><span class="line"> https:&#x2F;&#x2F;hub.docker.com&#x2F;</span><br><span class="line">For more examples and ideas, visit:</span><br><span class="line"> https:&#x2F;&#x2F;docs.docker.com&#x2F;engine&#x2F;userguide&#x2F;</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS下安装docker-compose记录</title>
    <url>/2019/12/22/CentOS%E4%B8%8B%E5%AE%89%E8%A3%85docker-compose%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="安装方式"><a href="#安装方式" class="headerlink" title="安装方式"></a>安装方式</h3><blockquote>
<p>Linux下安装</p>
</blockquote>
<ol>
<li><p>运行如下命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo curl -L &quot;https:&#x2F;&#x2F;github.com&#x2F;docker&#x2F;compose&#x2F;releases&#x2F;download&#x2F;1.25.0&#x2F;docker-compose-$(uname -s)-$(uname -m)&quot; -o &#x2F;usr&#x2F;local&#x2F;bin&#x2F;docker-compose</span><br></pre></td></tr></table></figure>
<blockquote>
<p>要安装其他版本可以替换<code>1.25.0</code></p>
</blockquote>
</li>
<li><p>赋予执行可执行权限</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo chmod +x &#x2F;usr&#x2F;local&#x2F;bin&#x2F;docker-compose</span><br></pre></td></tr></table></figure></li>
<li><p>测试安装结果</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ docker-compose -v</span><br><span class="line">docker-compose version 1.25.0, build 0a186604</span><br></pre></td></tr></table></figure>


</li>
</ol>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker私有仓库搭建记录</title>
    <url>/2019/07/18/Docker%E7%A7%81%E6%9C%89%E4%BB%93%E5%BA%93%E6%90%AD%E5%BB%BA%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="安装运行-docker-registry"><a href="#安装运行-docker-registry" class="headerlink" title="安装运行 docker-registry"></a>安装运行 docker-registry</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -d --name registry --restart&#x3D;always \</span><br><span class="line">-p 10.12.64.80:5000:5000 \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;registry&#x2F;0&#x2F;:&#x2F;var&#x2F;lib&#x2F;registry \</span><br><span class="line">registry</span><br></pre></td></tr></table></figure>
<h3 id="使用本地仓库（以mysql为例）"><a href="#使用本地仓库（以mysql为例）" class="headerlink" title="使用本地仓库（以mysql为例）"></a>使用本地仓库（以mysql为例）</h3><ol>
<li><p>生成mysql的tag</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker tag mysql:5.7.23 registry:5000&#x2F;mysql:5.7.23</span><br></pre></td></tr></table></figure></li>
<li><p>将mysql推入私有仓库</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker push registry:5000&#x2F;mysql:5.7.23</span><br></pre></td></tr></table></figure>
<blockquote>
<p>如果出现如下提示，参考备注(2)<br>The push refers to repository [registry:5000/mysql]<br>Get <a href="https://registry:5000/v2/">https://registry:5000/v2/</a>: http: server gave HTTP response to HTTPS client</p>
</blockquote>
</li>
<li><p>从本地仓库获取镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --restart&#x3D;always --name mysql-prod \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;mysql&#x2F;3&#x2F;db:&#x2F;var&#x2F;lib&#x2F;mysql \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;mysql&#x2F;3&#x2F;conf:&#x2F;etc&#x2F;mysql&#x2F;conf.d \</span><br><span class="line">-e MYSQL_ROOT_PASSWORD&#x3D;123456 -d -p 10.12.64.60:3306:3306 registry:5000&#x2F;mysql:5.7.23</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h3><ol>
<li><p>registry:5000表示<strong>HOST:PORT</strong>格式</p>
</li>
<li><p>可以在**/etc/hosts**中配置registry对应的IP地址</p>
</li>
<li><p>编辑**/etc/docker/daemon.json**添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&quot;insecure-registries&quot;:[&quot;registry:5000&quot;]&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>查看私有仓库镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">curl -XGET registry:5000&#x2F;v2&#x2F;_catalog</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Git配置自定义日志格式</title>
    <url>/2020/02/05/Git%E9%85%8D%E7%BD%AE%E8%87%AA%E5%AE%9A%E4%B9%89%E6%97%A5%E5%BF%97%E6%A0%BC%E5%BC%8F/</url>
    <content><![CDATA[<blockquote>
<p>Git配置自定义日志格式</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global alias.log2 &quot;log --pretty&#x3D;format:&#39;%Cred%h%Creset %C(bold black)%p %C(bold cyan)%d%Cblue%Creset %s %Cgreen%cd %C(blue)%an%Creset&#39; --date&#x3D;iso&quot;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>SCM</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>IDEA-Find-in-Path找不到文件的解决方法</title>
    <url>/2019/05/09/IDEA-Find-in-Path%E6%89%BE%E4%B8%8D%E5%88%B0%E6%96%87%E4%BB%B6%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<blockquote>
<p>IntelliJ IDEA 版本 2017.1.2</p>
</blockquote>
<ol>
<li><strong><em>File</em></strong>菜单下**<em>Invalidate Caches / Restart**</em></li>
<li>选择按钮**<em>Invalidate and Restart**</em></li>
</ol>
]]></content>
      <categories>
        <category>IDE</category>
      </categories>
      <tags>
        <tag>IntelliJ IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx的配置笔记</title>
    <url>/2019/10/16/Nginx%E7%9A%84%E9%85%8D%E7%BD%AE%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h3 id="配置反向代理并支持跨域请求"><a href="#配置反向代理并支持跨域请求" class="headerlink" title="配置反向代理并支持跨域请求"></a>配置反向代理并支持跨域请求</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">location ^~ &#x2F;api&#x2F; &#123;</span><br><span class="line">	# Check if the origin of th request</span><br><span class="line">	set $cors &#39;on&#39;;</span><br><span class="line"></span><br><span class="line">	if ($request_method &#x3D; OPTIONS) &#123;</span><br><span class="line">		set $cors &quot;$&#123;cors&#125;_options&quot;;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	# Allow CORS on preflight request</span><br><span class="line">	if ($cors &#x3D; &#39;on_options&#39;) &#123;</span><br><span class="line">		add_header &#39;Content-Length&#39; 0;</span><br><span class="line">		add_header &#39;Content-Type&#39; &#39;text&#x2F;plain; charset&#x3D;utf-8&#39;;</span><br><span class="line">		# 跨域时允许的来源地址</span><br><span class="line">		add_header &#39;Access-Control-Allow-Origin&#39; &quot;$http_origin&quot;;</span><br><span class="line">		# 跨域时允许的请求类型</span><br><span class="line">		add_header &#39;Access-Control-Allow-Methods&#39; &#39;GET, POST, OPTIONS&#39;;</span><br><span class="line">		# 跨域时允许提交的请求头字段</span><br><span class="line">		add_header &#39;Access-Control-Allow-Headers&#39; &#39;Origin, X-Requested-With, Content-Type, Accept&#39;;</span><br><span class="line">		return 204;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	# Proxy pass to upstream</span><br><span class="line">	proxy_pass http:&#x2F;&#x2F;192.168.1.100:8080;</span><br><span class="line">	proxy_redirect     off;</span><br><span class="line">	proxy_set_header   Host $host;</span><br><span class="line">	proxy_set_header   X-Real-IP $remote_addr;</span><br><span class="line">	proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;</span><br><span class="line">	proxy_set_header   X-Forwarded-Host $server_name;</span><br><span class="line"></span><br><span class="line">	# Allow CORS on other requests after returning from the upstreams</span><br><span class="line">	if ($cors &#x3D; &#39;on&#39;) &#123;</span><br><span class="line">		add_header &#39;Access-Control-Allow-Origin&#39; &quot;$http_origin&quot;;</span><br><span class="line">		add_header &#39;Access-Control-Allow-Methods&#39; &#39;GET, POST, OPTIONS&#39;;</span><br><span class="line">		add_header &#39;Access-Control-Allow-Headers&#39; &#39;Origin, X-Requested-With, Content-Type, Accept&#39;;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>参考地址:</p>
<ol>
<li><a href="https://enable-cors.org/">https://enable-cors.org</a></li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS">https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS</a></li>
<li><a href="https://blog.boatswain.io/post/setup-cors-in-nginx-with-proxypass-upstream/">https://blog.boatswain.io/post/setup-cors-in-nginx-with-proxypass-upstream/</a></li>
</ol>
</blockquote>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>POM文件内容介绍</title>
    <url>/2019/08/23/POM%E6%96%87%E4%BB%B6%E5%86%85%E5%AE%B9%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version&#x3D;&quot;1.0&quot; encoding&#x3D;&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;project xmlns&#x3D;&quot;http:&#x2F;&#x2F;maven.apache.org&#x2F;POM&#x2F;4.0.0&quot; xmlns:xsi&#x3D;&quot;http:&#x2F;&#x2F;www.w3.org&#x2F;2001&#x2F;XMLSchema-instance&quot;</span><br><span class="line">         xsi:schemaLocation&#x3D;&quot;http:&#x2F;&#x2F;maven.apache.org&#x2F;POM&#x2F;4.0.0 http:&#x2F;&#x2F;maven.apache.org&#x2F;xsd&#x2F;maven-4.0.0.xsd&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;modelVersion&gt;4.0.0&lt;&#x2F;modelVersion&gt;</span><br><span class="line">    &lt;!--组件坐标&lt;groupId&gt;&lt;artifactId&gt;&lt;version&gt;--&gt;</span><br><span class="line">    &lt;groupId&gt;cn.realdev&lt;&#x2F;groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;pattern-demo&lt;&#x2F;artifactId&gt;</span><br><span class="line">    &lt;version&gt;1.0-SNAPSHOT&lt;&#x2F;version&gt;</span><br><span class="line">    &lt;name&gt;pattern-demo&lt;&#x2F;name&gt;</span><br><span class="line">    &lt;url&gt;http:&#x2F;&#x2F;www.example.com&lt;&#x2F;url&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--自定义属性变量--&gt;</span><br><span class="line">    &lt;properties&gt;</span><br><span class="line">        &lt;maven.compiler.source&gt;1.7&lt;&#x2F;maven.compiler.source&gt;</span><br><span class="line">        &lt;maven.compiler.target&gt;1.7&lt;&#x2F;maven.compiler.target&gt;</span><br><span class="line">        &lt;project.build.sourceEncoding&gt;UTF-8&lt;&#x2F;project.build.sourceEncoding&gt;</span><br><span class="line">    &lt;&#x2F;properties&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--当前组件的依赖组件--&gt;</span><br><span class="line">    &lt;dependencies&gt;</span><br><span class="line">        &lt;dependency&gt;</span><br><span class="line">            &lt;groupId&gt;junit&lt;&#x2F;groupId&gt;</span><br><span class="line">            &lt;artifactId&gt;junit&lt;&#x2F;artifactId&gt;</span><br><span class="line">            &lt;version&gt;4.11&lt;&#x2F;version&gt;</span><br><span class="line">            &lt;scope&gt;test&lt;&#x2F;scope&gt;</span><br><span class="line">        &lt;&#x2F;dependency&gt;</span><br><span class="line">    &lt;&#x2F;dependencies&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--远程仓库列表--&gt;</span><br><span class="line">    &lt;repositories&gt;</span><br><span class="line">        &lt;repository&gt;</span><br><span class="line">            &lt;snapshots&gt;</span><br><span class="line">                &lt;enabled&gt;false&lt;&#x2F;enabled&gt;</span><br><span class="line">            &lt;&#x2F;snapshots&gt;</span><br><span class="line">            &lt;id&gt;aliyun-repos&lt;&#x2F;id&gt;</span><br><span class="line">            &lt;url&gt;http:&#x2F;&#x2F;maven.aliyun.com&#x2F;nexus&#x2F;content&#x2F;groups&#x2F;public&#x2F;&lt;&#x2F;url&gt;</span><br><span class="line">        &lt;&#x2F;repository&gt;</span><br><span class="line">        &lt;repository&gt;</span><br><span class="line">            &lt;snapshots&gt;</span><br><span class="line">                &lt;enabled&gt;false&lt;&#x2F;enabled&gt;</span><br><span class="line">            &lt;&#x2F;snapshots&gt;</span><br><span class="line">            &lt;id&gt;jcenter&lt;&#x2F;id&gt;</span><br><span class="line">            &lt;url&gt;http:&#x2F;&#x2F;jcenter.bintray.com&lt;&#x2F;url&gt;</span><br><span class="line">        &lt;&#x2F;repository&gt;</span><br><span class="line">        &lt;repository&gt;</span><br><span class="line">            &lt;snapshots&gt;</span><br><span class="line">                &lt;enabled&gt;false&lt;&#x2F;enabled&gt;</span><br><span class="line">            &lt;&#x2F;snapshots&gt;</span><br><span class="line">            &lt;id&gt;central&lt;&#x2F;id&gt;</span><br><span class="line">            &lt;name&gt;Central Repository&lt;&#x2F;name&gt;</span><br><span class="line">            &lt;url&gt;https:&#x2F;&#x2F;repo.maven.apache.org&#x2F;maven2&lt;&#x2F;url&gt;</span><br><span class="line">        &lt;&#x2F;repository&gt;</span><br><span class="line">    &lt;&#x2F;repositories&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--插件仓库列表--&gt;</span><br><span class="line">    &lt;pluginRepositories&gt;</span><br><span class="line">        &lt;pluginRepository&gt;</span><br><span class="line">            &lt;releases&gt;</span><br><span class="line">                &lt;updatePolicy&gt;never&lt;&#x2F;updatePolicy&gt;</span><br><span class="line">            &lt;&#x2F;releases&gt;</span><br><span class="line">            &lt;snapshots&gt;</span><br><span class="line">                &lt;enabled&gt;false&lt;&#x2F;enabled&gt;</span><br><span class="line">            &lt;&#x2F;snapshots&gt;</span><br><span class="line">            &lt;id&gt;central&lt;&#x2F;id&gt;</span><br><span class="line">            &lt;name&gt;Central Repository&lt;&#x2F;name&gt;</span><br><span class="line">            &lt;url&gt;https:&#x2F;&#x2F;repo.maven.apache.org&#x2F;maven2&lt;&#x2F;url&gt;</span><br><span class="line">        &lt;&#x2F;pluginRepository&gt;</span><br><span class="line">    &lt;&#x2F;pluginRepositories&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--与构建相关的配置--&gt;</span><br><span class="line">    &lt;build&gt;</span><br><span class="line">        &lt;sourceDirectory&gt;D:\pattern-demo\src\main\java&lt;&#x2F;sourceDirectory&gt;</span><br><span class="line">        &lt;scriptSourceDirectory&gt;D:\pattern-demo\src\main\scripts&lt;&#x2F;scriptSourceDirectory&gt;</span><br><span class="line">        &lt;testSourceDirectory&gt;D:\pattern-demo\src\test\java&lt;&#x2F;testSourceDirectory&gt;</span><br><span class="line">        &lt;outputDirectory&gt;D:\pattern-demo\target\classes&lt;&#x2F;outputDirectory&gt;</span><br><span class="line">        &lt;testOutputDirectory&gt;D:\pattern-demo\target\test-classes&lt;&#x2F;testOutputDirectory&gt;</span><br><span class="line">        &lt;resources&gt;</span><br><span class="line">            &lt;resource&gt;</span><br><span class="line">                &lt;directory&gt;D:\pattern-demo\src\main\resources&lt;&#x2F;directory&gt;</span><br><span class="line">            &lt;&#x2F;resource&gt;</span><br><span class="line">        &lt;&#x2F;resources&gt;</span><br><span class="line">        &lt;testResources&gt;</span><br><span class="line">            &lt;testResource&gt;</span><br><span class="line">                &lt;directory&gt;D:\pattern-demo\src\test\resources&lt;&#x2F;directory&gt;</span><br><span class="line">            &lt;&#x2F;testResource&gt;</span><br><span class="line">        &lt;&#x2F;testResources&gt;</span><br><span class="line">        &lt;directory&gt;D:\pattern-demo\target&lt;&#x2F;directory&gt;</span><br><span class="line">        &lt;finalName&gt;pattern-demo-1.0-SNAPSHOT&lt;&#x2F;finalName&gt;</span><br><span class="line">        &lt;pluginManagement&gt;</span><br><span class="line">            &lt;plugins&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-antrun-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;1.3&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-assembly-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.2-beta-5&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-dependency-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.8&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-release-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.5.3&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-clean-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.1.0&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-resources-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.0.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-compiler-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.8.0&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-surefire-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.22.1&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-jar-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.0.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-install-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.5.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-deploy-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;2.8.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-site-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.7.1&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">                &lt;plugin&gt;</span><br><span class="line">                    &lt;artifactId&gt;maven-project-info-reports-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                    &lt;version&gt;3.0.0&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;&#x2F;plugins&gt;</span><br><span class="line">        &lt;&#x2F;pluginManagement&gt;</span><br><span class="line">        &lt;plugins&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-clean-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;3.1.0&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-clean&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;clean&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;clean&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-resources-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;3.0.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-testResources&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;process-test-resources&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;testResources&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-resources&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;process-resources&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;resources&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-jar-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;3.0.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-jar&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;package&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;jar&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-compiler-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;3.8.0&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-compile&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;compile&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;compile&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-testCompile&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;test-compile&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;testCompile&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-surefire-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;2.22.1&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-test&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;test&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;test&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-install-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;2.5.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-install&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;install&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;install&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-deploy-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;2.8.2&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-deploy&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;deploy&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;deploy&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">            &lt;plugin&gt;</span><br><span class="line">                &lt;artifactId&gt;maven-site-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                &lt;version&gt;3.7.1&lt;&#x2F;version&gt;</span><br><span class="line">                &lt;executions&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-site&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;site&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;site&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                        &lt;configuration&gt;</span><br><span class="line">                            &lt;outputDirectory&gt;D:\pattern-demo\target\site&lt;&#x2F;outputDirectory&gt;</span><br><span class="line">                            &lt;reportPlugins&gt;</span><br><span class="line">                                &lt;reportPlugin&gt;</span><br><span class="line">                                    &lt;groupId&gt;org.apache.maven.plugins&lt;&#x2F;groupId&gt;</span><br><span class="line">                                    &lt;artifactId&gt;maven-project-info-reports-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                                &lt;&#x2F;reportPlugin&gt;</span><br><span class="line">                            &lt;&#x2F;reportPlugins&gt;</span><br><span class="line">                        &lt;&#x2F;configuration&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                    &lt;execution&gt;</span><br><span class="line">                        &lt;id&gt;default-deploy&lt;&#x2F;id&gt;</span><br><span class="line">                        &lt;phase&gt;site-deploy&lt;&#x2F;phase&gt;</span><br><span class="line">                        &lt;goals&gt;</span><br><span class="line">                            &lt;goal&gt;deploy&lt;&#x2F;goal&gt;</span><br><span class="line">                        &lt;&#x2F;goals&gt;</span><br><span class="line">                        &lt;configuration&gt;</span><br><span class="line">                            &lt;outputDirectory&gt;D:\pattern-demo\target\site&lt;&#x2F;outputDirectory&gt;</span><br><span class="line">                            &lt;reportPlugins&gt;</span><br><span class="line">                                &lt;reportPlugin&gt;</span><br><span class="line">                                    &lt;groupId&gt;org.apache.maven.plugins&lt;&#x2F;groupId&gt;</span><br><span class="line">                                    &lt;artifactId&gt;maven-project-info-reports-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                                &lt;&#x2F;reportPlugin&gt;</span><br><span class="line">                            &lt;&#x2F;reportPlugins&gt;</span><br><span class="line">                        &lt;&#x2F;configuration&gt;</span><br><span class="line">                    &lt;&#x2F;execution&gt;</span><br><span class="line">                &lt;&#x2F;executions&gt;</span><br><span class="line">                &lt;configuration&gt;</span><br><span class="line">                    &lt;outputDirectory&gt;D:\pattern-demo\target\site&lt;&#x2F;outputDirectory&gt;</span><br><span class="line">                    &lt;reportPlugins&gt;</span><br><span class="line">                        &lt;reportPlugin&gt;</span><br><span class="line">                            &lt;groupId&gt;org.apache.maven.plugins&lt;&#x2F;groupId&gt;</span><br><span class="line">                            &lt;artifactId&gt;maven-project-info-reports-plugin&lt;&#x2F;artifactId&gt;</span><br><span class="line">                        &lt;&#x2F;reportPlugin&gt;</span><br><span class="line">                    &lt;&#x2F;reportPlugins&gt;</span><br><span class="line">                &lt;&#x2F;configuration&gt;</span><br><span class="line">            &lt;&#x2F;plugin&gt;</span><br><span class="line">        &lt;&#x2F;plugins&gt;</span><br><span class="line">    &lt;&#x2F;build&gt;</span><br><span class="line"></span><br><span class="line">    &lt;reporting&gt;</span><br><span class="line">        &lt;outputDirectory&gt;D:\pattern-demo\target\site&lt;&#x2F;outputDirectory&gt;</span><br><span class="line">    &lt;&#x2F;reporting&gt;</span><br><span class="line">&lt;&#x2F;project&gt;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Backend</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>IntelliJ-IDEA-快捷键整理</title>
    <url>/2019/05/09/IntelliJ-IDEA-%E5%BF%AB%E6%8D%B7%E9%94%AE%E6%95%B4%E7%90%86/</url>
    <content><![CDATA[<table>
<thead>
<tr>
<th>Win</th>
<th>常规</th>
</tr>
</thead>
<tbody><tr>
<td><strong><em>Ctrl+Shift+Enter</em></strong></td>
<td>语句完成、光标所在行的上方插入空行</td>
</tr>
<tr>
<td><strong><em>Ctrl+E</em></strong></td>
<td>最近的文件</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+E</em></strong></td>
<td>最近更改的文件</td>
</tr>
<tr>
<td><strong><em>Ctrl+[</em></strong></td>
<td>大括号的开始</td>
</tr>
<tr>
<td><strong><em>Ctrl+]</em></strong></td>
<td>大括号的结束</td>
</tr>
<tr>
<td><strong><em>Ctrl+F12</em></strong></td>
<td>显示当前文件结构，列出类及类方法</td>
</tr>
<tr>
<td><strong><em>Ctrl+F7</em></strong></td>
<td>可以查询当前元素在当前文件中的引用（可与F3、Shift+F3配合使用）</td>
</tr>
<tr>
<td><strong><em>Ctrl+N</em></strong></td>
<td>快速类查找</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+N</em></strong></td>
<td>快速文件查找</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+Alt+N</em></strong></td>
<td>快速符号查找</td>
</tr>
<tr>
<td><strong><em>Ctrl+P</em></strong></td>
<td>可以显示参数信息</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+Insert</em></strong></td>
<td>可以选择剪贴板内容并插入</td>
</tr>
<tr>
<td><strong><em>Alt+Insert</em></strong></td>
<td>打开代码自动生成菜单</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+V</em></strong></td>
<td>自动胜场变量定义</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+T</em></strong></td>
<td>对选中的代码进行包装：try…catch…finally等等</td>
</tr>
<tr>
<td><strong><em>Ctrl+Enter</em></strong></td>
<td>光标所在行的下方插入空行</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+L</em></strong></td>
<td>格式化代码</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+O</em></strong></td>
<td>优化导入的类和包</td>
</tr>
<tr>
<td><strong><em>Ctrl+R</em></strong></td>
<td>替换文本</td>
</tr>
<tr>
<td><strong><em>Ctrl+F</em></strong></td>
<td>查找当前文本</td>
</tr>
<tr>
<td><strong><em>F3</em></strong></td>
<td>查找下一个（定位下一个）</td>
</tr>
<tr>
<td><strong><em>Shift+F3</em></strong></td>
<td>查找上一个（定位上一个）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+Space</em></strong></td>
<td>建议代码</td>
</tr>
<tr>
<td><strong><em>Ctrl+Space</em></strong></td>
<td>代码提示（与系统输入法快捷键冲突）</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+Up&#124;Down</em></strong></td>
<td>光标所在行的代码行上移或下移一行</td>
</tr>
<tr>
<td><strong><em>Ctrl+Y</em></strong></td>
<td>删除行</td>
</tr>
<tr>
<td><strong><em>Ctrl+D</em></strong></td>
<td>复制行</td>
</tr>
<tr>
<td><strong><em>Ctrl+X</em></strong></td>
<td>剪切</td>
</tr>
<tr>
<td><strong><em>Ctrl+/</em></strong></td>
<td>行注释</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+/</em></strong></td>
<td>块注释</td>
</tr>
<tr>
<td><strong><em>Ctrl+J</em></strong></td>
<td>自动代码片段</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+J</em></strong></td>
<td>动态环绕模板</td>
</tr>
<tr>
<td><strong><em>Ctrl+H</em></strong></td>
<td>类的继承层次</td>
</tr>
<tr>
<td><strong><em>Ctrl+Q</em></strong></td>
<td>显示注释文档</td>
</tr>
<tr>
<td><strong><em>Alt+F1</em></strong></td>
<td>目标选择器（可以定位文件所在项目位置）</td>
</tr>
<tr>
<td><strong><em>Alt+1</em></strong></td>
<td>快速打开或隐藏工程面板</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Left&#124;Right</em></strong></td>
<td>返回至上次浏览的位置</td>
</tr>
<tr>
<td><strong><em>Alt+Left&#124;Right</em></strong></td>
<td>切换代码视图</td>
</tr>
<tr>
<td><strong><em>Alt+Up&#124;Down</em></strong></td>
<td>在方法间快速移动定位</td>
</tr>
<tr>
<td><strong><em>F2&#124;Shift+F2</em></strong></td>
<td>高亮错误或警告快速定位</td>
</tr>
<tr>
<td><strong><em>Tab</em></strong></td>
<td>代码标签输入完成后，按Tab生成代码</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F7</em></strong></td>
<td>高亮光标所在变量、方法等及其引用点（可与F3、Shift+F3配合使用）；如果为选中文本时，则与Alt+F3等效；</td>
</tr>
<tr>
<td><strong><em>Alt+F3</em></strong></td>
<td>选中的文本内容在文本查询框中（可与F3、Shift+F3配合使用）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Up&#124;Down</em></strong></td>
<td>当前屏幕上下滚动</td>
</tr>
<tr>
<td><strong><em>Ctrl+B</em></strong></td>
<td>可以在声明和引用之间切换</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+B</em></strong></td>
<td>跳转到方法实现处</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+Backspace</em></strong></td>
<td>跳转到上次编辑的地方</td>
</tr>
<tr>
<td><strong><em>Ctrl+O</em></strong></td>
<td>快速重写(Override)方法</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Space</em></strong></td>
<td>检索匹配内容</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+J</em></strong></td>
<td>将下一行与当前行合并成一行</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+V</em></strong></td>
<td>可以将最近使用的剪贴板内容选择插入到文本</td>
</tr>
<tr>
<td><strong><em>Shift+Esc</em></strong></td>
<td>隐藏活动的工具窗口</td>
</tr>
<tr>
<td><strong><em>F12</em></strong></td>
<td>把焦点从编辑器移到最近使用的工具窗口</td>
</tr>
<tr>
<td><strong><em>Ctrl+W</em></strong></td>
<td>可以选择单词继而语句继而行继而函数</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+W</em></strong></td>
<td>取消选择光标所在词</td>
</tr>
<tr>
<td><strong><em>Alt+F7</em></strong></td>
<td>从整个工程范围里查找选中的类型、方法、变量的引用并在小窗中显示</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+F7</em></strong></td>
<td>从整个工程范围里查找选中的类型、方法、变量的引用并弹框显示，如果唯一则直接定位至引用处</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+U</em></strong></td>
<td>大小写转化</td>
</tr>
<tr>
<td><strong><em>Shift+Enter</em></strong></td>
<td>向下插入新行</td>
</tr>
<tr>
<td><strong><em>Shift+F4</em></strong></td>
<td>以独立窗口方式打开当前编辑的文件</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F</em></strong></td>
<td>全局查找</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+R</em></strong></td>
<td>全局替换</td>
</tr>
<tr>
<td><strong><em>Ctrl+U</em></strong></td>
<td>转到父类</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+S</em></strong></td>
<td>打开设置对话框</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Shift+S</em></strong></td>
<td>打开当前项目结构</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+Insert</em></strong></td>
<td>列选择模式开关</td>
</tr>
<tr>
<td><strong><em>Ctrl+G</em></strong></td>
<td>定位行列</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Enter</em></strong></td>
<td>上插一行</td>
</tr>
<tr>
<td><strong><em>Ctrl+Backspace</em></strong></td>
<td>按单词删除</td>
</tr>
<tr>
<td><strong><em>Ctrl+plus&#124;minus</em></strong></td>
<td>当前方法展开、折叠</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+plus&#124;minus</em></strong></td>
<td>全部展开、折叠</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+U</em></strong></td>
<td>显示当前类图（弹框形式展示）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Shift+U</em></strong></td>
<td>显示当前类图（卡片形式展示）</td>
</tr>
<tr>
<td><strong><em>Alt+F12</em></strong></td>
<td>显示Terminal窗口</td>
</tr>
<tr>
<td><strong><em>Alt+0</em></strong></td>
<td>显示Message窗口</td>
</tr>
<tr>
<td><strong><em>Alt+1</em></strong></td>
<td>显示Project窗口</td>
</tr>
<tr>
<td><strong><em>Alt+2</em></strong></td>
<td>显示Favorites窗口</td>
</tr>
<tr>
<td><strong><em>Alt+5</em></strong></td>
<td>显示Debug窗口</td>
</tr>
<tr>
<td><strong><em>Alt+6</em></strong></td>
<td>显示Todo窗口</td>
</tr>
<tr>
<td><strong><em>Alt+7</em></strong></td>
<td>显示Structure窗口</td>
</tr>
<tr>
<td><strong><em>Alt+9</em></strong></td>
<td>显示VCS窗口</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+1~9</em></strong></td>
<td>给代码行打书签</td>
</tr>
<tr>
<td><strong><em>Ctrl+1~9</em></strong></td>
<td>快速定位代码行书签</td>
</tr>
<tr>
<td><strong><em>Ctrl+.</em></strong></td>
<td>收拢和展开当前方法体</td>
</tr>
<tr>
<td><strong><em>Ctrl+F2</em></strong></td>
<td>停止当前的进程</td>
</tr>
<tr>
<td><strong><em>Ctrl+F9</em></strong></td>
<td>生成项目</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+C</em></strong></td>
<td>复制文件全路径</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Shift+C</em></strong></td>
<td>复制类全名</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Y</em></strong></td>
<td>同步</td>
</tr>
<tr>
<td><strong><em>Ctrl+~</em></strong></td>
<td>快速切换方案（界面外观、代码风格、快捷键映射等菜单）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F12</em></strong></td>
<td>隐藏/恢复所有窗口</td>
</tr>
<tr>
<td><strong><em>Ctrl+F4</em></strong></td>
<td>关闭当前窗口</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F4</em></strong></td>
<td>关闭活动选项卡</td>
</tr>
<tr>
<td><strong><em>Ctrl+Tab</em></strong></td>
<td>快捷切换（正序）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+Tab</em></strong></td>
<td>快捷切换（反序）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+N</em></strong></td>
<td>内联</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+T</em></strong></td>
<td>创建单元测试</td>
</tr>
<tr>
<td><strong><em>CtrlShift+F10</em></strong></td>
<td>运行单元测试</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+M</em></strong></td>
<td>光标所在语句块开始与结尾处切换</td>
</tr>
<tr>
<td><strong><em>Alt+Enter</em></strong></td>
<td>语法，语言，文本注入</td>
</tr>
<tr>
<td><strong><em>Ctrl+M</em></strong></td>
<td>返回光标所在位置</td>
</tr>
<tr>
<td><strong><em>Ctrl+Ctrl</em></strong></td>
<td>快捷运行任何命令</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+I</em></strong></td>
<td>代码规范检测</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>Win</th>
<th>重构</th>
</tr>
</thead>
<tbody><tr>
<td><strong><em>F6</em></strong></td>
<td>移动</td>
</tr>
<tr>
<td><strong><em>F5</em></strong></td>
<td>复制</td>
</tr>
<tr>
<td><strong><em>Shift+F6</em></strong></td>
<td>重构、重命名</td>
</tr>
<tr>
<td><strong><em>Ctrl+F6</em></strong></td>
<td>变更方法签名</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+M</em></strong></td>
<td>将选中的代码提取成一个方法</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+P</em></strong></td>
<td>以选中的变量为依据重构方法的入参</td>
</tr>
<tr>
<td><strong><em>Alt+Delete</em></strong></td>
<td>安全删除（检索注释、字符串和文本）</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Shift+T</em></strong></td>
<td>显示重构菜单</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>Win</th>
<th>调试</th>
</tr>
</thead>
<tbody><tr>
<td><strong><em>Alt+F8</em></strong></td>
<td>计算变量值</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+F9</em></strong></td>
<td>以Debug方式启动…</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+F10</em></strong></td>
<td>以Run方式启动…</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F9</em></strong></td>
<td>编译</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F10</em></strong></td>
<td>运行</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+F8</em></strong></td>
<td>查看断点</td>
</tr>
<tr>
<td><strong><em>F8</em></strong></td>
<td>步过</td>
</tr>
<tr>
<td><strong><em>F7</em></strong></td>
<td>步入</td>
</tr>
<tr>
<td><strong><em>Shift+F7</em></strong></td>
<td>智能步入</td>
</tr>
<tr>
<td><strong><em>Shift+F8</em></strong></td>
<td>步出</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+F8</em></strong></td>
<td>强制步过</td>
</tr>
<tr>
<td><strong><em>Alt+Shift+F7</em></strong></td>
<td>强制步入</td>
</tr>
<tr>
<td><strong><em>Alt+F9</em></strong></td>
<td>运行至光标处</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+F9</em></strong></td>
<td>强制运行至光标处</td>
</tr>
<tr>
<td><strong><em>F9</em></strong></td>
<td>恢复程序（恢复运行）</td>
</tr>
<tr>
<td><strong><em>Alt+F10</em></strong></td>
<td>定位至断点</td>
</tr>
<tr>
<td><strong><em>Ctrl+F8</em></strong></td>
<td>断点开关切换</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>Win</th>
<th>版本控制</th>
</tr>
</thead>
<tbody><tr>
<td><strong><em>Alt+Shift+C</em></strong></td>
<td>最近的更改</td>
</tr>
<tr>
<td><strong><em>Alt+~</em></strong></td>
<td>弹出VCS菜单</td>
</tr>
<tr>
<td><strong><em>Ctrl+T</em></strong></td>
<td>更新</td>
</tr>
<tr>
<td><strong><em>Ctrl+K</em></strong></td>
<td>提交</td>
</tr>
<tr>
<td><strong><em>Ctrl+Alt+Shift+D</em></strong></td>
<td>显示变化</td>
</tr>
<tr>
<td><strong><em>Ctrl+Shift+`</em></strong></td>
<td>显示版本管理分支弹窗</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>IDE</category>
      </categories>
      <tags>
        <tag>IntelliJ IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2019/05/07/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>Zabbix安装记录</title>
    <url>/2021/02/03/Zabbix%E5%AE%89%E8%A3%85%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><p>按照<a href="https://www.zabbix.com/download">官方网站</a>的流程选择合适的版本。当前使用的是<code>Zabbix Packages</code>途径安装。</p>
<table>
<thead>
<tr>
<th>ZABBIX VERSION</th>
<th>OS DISTRIBUTION</th>
<th>OS VERSION</th>
<th>DATABASE</th>
<th>WEB SERVER</th>
</tr>
</thead>
<tbody><tr>
<td>5.0 LTS</td>
<td>CentOS</td>
<td>7</td>
<td>MySQL</td>
<td>NGINX</td>
</tr>
</tbody></table>
<blockquote>
<p>安装Zabbix仓库</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -Uvh https://repo.zabbix.com/zabbix/5.0/rhel/7/x86_64/zabbix-release-5.0-1.el7.noarch.rpm</span><br><span class="line">yum clean all</span><br></pre></td></tr></table></figure>
<blockquote>
<p>安装Zabbix服务与代理</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install zabbix-server-mysql zabbix-agent</span><br></pre></td></tr></table></figure>
<h4 id="安装Zabbix前端界面"><a href="#安装Zabbix前端界面" class="headerlink" title="安装Zabbix前端界面"></a>安装Zabbix前端界面</h4><blockquote>
<p>安装红帽软件包</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install centos-release-scl</span><br></pre></td></tr></table></figure>
<blockquote>
<p>编辑<code>/etc/yum.repos.d/zabbix.repo</code>开启前端仓库</p>
</blockquote>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">[zabbix-frontend]</span></span><br><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="attr">enabled</span>=<span class="string">1</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>安装前端包</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install zabbix-web-mysql-scl zabbix-nginx-conf-scl</span><br></pre></td></tr></table></figure>

<h3 id="安装数据库"><a href="#安装数据库" class="headerlink" title="安装数据库"></a>安装数据库</h3><blockquote>
<p>数据库版本5.5.68-MariaDB</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install mariadb-server</span><br><span class="line">systemctl start mariadb</span><br><span class="line">systemctl <span class="built_in">enable</span> mariadb</span><br></pre></td></tr></table></figure>
<blockquote>
<p>数据库初始配置</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql_secure_installation</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">NOTE: RUNNING ALL PARTS OF THIS SCRIPT IS RECOMMENDED FOR ALL MariaDB</span><br><span class="line">      SERVERS IN PRODUCTION USE!  PLEASE READ EACH STEP CAREFULLY!</span><br><span class="line"></span><br><span class="line">In order to log into MariaDB to secure it, we&#39;ll need the current</span><br><span class="line">password for the root user.  If you&#39;ve just installed MariaDB, and</span><br><span class="line">you haven&#39;t set the root password yet, the password will be blank,</span><br><span class="line">so you should just press enter here.</span><br><span class="line"></span><br><span class="line">Enter current password for root (enter for none): </span><br><span class="line">OK, successfully used password, moving on...</span><br><span class="line"></span><br><span class="line">Setting the root password ensures that nobody can log into the MariaDB</span><br><span class="line">root user without the proper authorisation.</span><br><span class="line"></span><br><span class="line">Set root password? [Y&#x2F;n] Y</span><br><span class="line">New password: </span><br><span class="line">Re-enter new password: </span><br><span class="line">Password updated successfully!</span><br><span class="line">Reloading privilege tables..</span><br><span class="line"> ... Success!</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">By default, a MariaDB installation has an anonymous user, allowing anyone</span><br><span class="line">to log into MariaDB without having to have a user account created for</span><br><span class="line">them.  This is intended only for testing, and to make the installation</span><br><span class="line">go a bit smoother.  You should remove them before moving into a</span><br><span class="line">production environment.</span><br><span class="line"></span><br><span class="line">Remove anonymous users? [Y&#x2F;n] Y</span><br><span class="line"> ... Success!</span><br><span class="line"></span><br><span class="line">Normally, root should only be allowed to connect from &#39;localhost&#39;.  This</span><br><span class="line">ensures that someone cannot guess at the root password from the network.</span><br><span class="line"></span><br><span class="line">Disallow root login remotely? [Y&#x2F;n] Y</span><br><span class="line"> ... Success!</span><br><span class="line"></span><br><span class="line">By default, MariaDB comes with a database named &#39;test&#39; that anyone can</span><br><span class="line">access.  This is also intended only for testing, and should be removed</span><br><span class="line">before moving into a production environment.</span><br><span class="line"></span><br><span class="line">Remove test database and access to it? [Y&#x2F;n] Y</span><br><span class="line"> - Dropping test database...</span><br><span class="line"> ... Success!</span><br><span class="line"> - Removing privileges on test database...</span><br><span class="line"> ... Success!</span><br><span class="line"></span><br><span class="line">Reloading the privilege tables will ensure that all changes made so far</span><br><span class="line">will take effect immediately.</span><br><span class="line"></span><br><span class="line">Reload privilege tables now? [Y&#x2F;n] Y</span><br><span class="line"> ... Success!</span><br><span class="line"></span><br><span class="line">Cleaning up...</span><br><span class="line"></span><br><span class="line">All done!  If you&#39;ve completed all of the above steps, your MariaDB</span><br><span class="line">installation should now be secure.</span><br><span class="line"></span><br><span class="line">Thanks for using MariaDB!</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="初始化数据库及Zabbix脚本"><a href="#初始化数据库及Zabbix脚本" class="headerlink" title="初始化数据库及Zabbix脚本"></a>初始化数据库及Zabbix脚本</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -uroot -p</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql&gt; create database zabbix character <span class="built_in">set</span> utf8 collate utf8_bin;</span><br><span class="line">mysql&gt; create user zabbix@localhost identified by <span class="string">&#x27;zabbix&#x27;</span>;</span><br><span class="line">mysql&gt; grant all privileges on zabbix.* to zabbix@localhost;</span><br><span class="line">mysql&gt; flush privileges;</span><br><span class="line">mysql&gt; quit;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zcat /usr/share/doc/zabbix-server-mysql*/create.sql.gz | mysql -uzabbix -p zabbix</span><br></pre></td></tr></table></figure>
<blockquote>
<p>配置Zabbix数据库信息<code>/etc/zabbix/zabbix_server.conf</code></p>
</blockquote>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="attr">DBPassword</span>=<span class="string">password</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>
<h4 id="配置Zabbix前端PHP信息"><a href="#配置Zabbix前端PHP信息" class="headerlink" title="配置Zabbix前端PHP信息"></a>配置Zabbix前端PHP信息</h4><blockquote>
<p>编辑<code>/etc/opt/rh/rh-nginx116/nginx/conf.d/zabbix.conf</code>，配置自定义域名或IP</p>
</blockquote>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="attr">listen</span> <span class="string">80;</span></span><br><span class="line"><span class="attr">server_name</span> <span class="string">example.com;</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>编辑<code>/etc/opt/rh/rh-php72/php-fpm.d/zabbix.conf</code>及设置正确的时区</p>
</blockquote>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="meta">listen.acl_users</span> = <span class="string">apache,nginx</span></span><br><span class="line"><span class="meta">php_value[date.timezone]</span> = <span class="string">Europe/Riga</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>
<h3 id="配置防火墙"><a href="#配置防火墙" class="headerlink" title="配置防火墙"></a>配置防火墙</h3><blockquote>
<p>HTTP端口放行</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-service=http --permanent</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>
<blockquote>
<p>临时设置SELinux工作模式设置</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setenforce 0</span><br></pre></td></tr></table></figure>
<h3 id="启动服务"><a href="#启动服务" class="headerlink" title="启动服务"></a>启动服务</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl restart zabbix-server zabbix-agent rh-nginx116-nginx rh-php72-php-fpm</span><br><span class="line">systemctl <span class="built_in">enable</span> zabbix-server zabbix-agent rh-nginx116-nginx rh-php72-php-fpm</span><br></pre></td></tr></table></figure>
<h3 id="登录Zabbix"><a href="#登录Zabbix" class="headerlink" title="登录Zabbix"></a>登录Zabbix</h3><blockquote>
<p>通过 <a href="http://server_ip_or_name/">http://server_ip_or_name</a> 访问并配置Zabbix</p>
</blockquote>
<p><img src="2021-02-04_113336.png"></p>
<p><img src="2021-02-04_113402.png"></p>
<p><img src="2021-02-04_113532.png"></p>
<p><img src="2021-02-04_113645.png"></p>
<p><img src="2021-02-04_113719.png"></p>
<p><img src="2021-02-04_113759.png"></p>
<p><img src="2021-02-04_113840.png"></p>
<h3 id="在Zabbix上启用和配置SELinux"><a href="#在Zabbix上启用和配置SELinux" class="headerlink" title="在Zabbix上启用和配置SELinux"></a>在Zabbix上启用和配置SELinux</h3><h4 id="允许http守护程序连接到Zabbix"><a href="#允许http守护程序连接到Zabbix" class="headerlink" title="允许http守护程序连接到Zabbix"></a>允许http守护程序连接到Zabbix</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setsebool -P httpd_can_connect_zabbix 1</span><br></pre></td></tr></table></figure>
<h4 id="允许Zabbix连接到所有TCP端口"><a href="#允许Zabbix连接到所有TCP端口" class="headerlink" title="允许Zabbix连接到所有TCP端口"></a>允许Zabbix连接到所有TCP端口</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setsebool -P zabbix_can_network 1</span><br></pre></td></tr></table></figure>
<h4 id="设置SELinux在强制模式下工作"><a href="#设置SELinux在强制模式下工作" class="headerlink" title="设置SELinux在强制模式下工作"></a>设置SELinux在强制模式下工作</h4><blockquote>
<p>开启强制模式</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setenforce 1 &amp;&amp; sed -i <span class="string">&#x27;s/^SELINUX=.*/SELINUX=enforcing/g&#x27;</span> /etc/selinux/config</span><br></pre></td></tr></table></figure>
<blockquote>
<p>检查SELinux状态(<em>Current mode: enforcing</em>)</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sestatus</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELinux status: enabled</span><br><span class="line">SELinuxfs mount: /sys/fs/selinux</span><br><span class="line">SELinux root directory: /etc/selinux</span><br><span class="line">Loaded policy name: targeted</span><br><span class="line">Current mode: enforcing</span><br><span class="line">Mode from config file: enforcing</span><br><span class="line">Policy MLS status: enabled</span><br><span class="line">Policy deny_unknown status: allowed</span><br><span class="line">Memory protection checking: actual (secure)</span><br><span class="line">Max kernel policy version: 31</span><br></pre></td></tr></table></figure>
<h4 id="为Zabbix创建SELinux补充策略"><a href="#为Zabbix创建SELinux补充策略" class="headerlink" title="为Zabbix创建SELinux补充策略"></a>为Zabbix创建SELinux补充策略</h4><blockquote>
<p>为审核日志中的每个错误创建SELinux补充策略。可能需要如下工具，如果没有先安装</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># CentOS8</span></span><br><span class="line">dnf -y install policycoreutils-python-utils</span><br><span class="line"><span class="comment"># Centos7</span></span><br><span class="line">yum -y install policycoreutils-python-utils</span><br></pre></td></tr></table></figure>
<blockquote>
<p>创建自定义策略包</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep <span class="string">&quot;denied.*zabbix&quot;</span> /var/<span class="built_in">log</span>/audit/audit.log | audit2allow -M zabbix_policy</span><br></pre></td></tr></table></figure>
<blockquote>
<p>安装自定义SELinux策略包</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">semodule -i zabbix_policy.pp</span><br></pre></td></tr></table></figure>
<blockquote>
<p>现在已经完成了为Zabbix配置SELinux工作</p>
</blockquote>
<h3 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h3><p>安装过程中碰到坑特此记录：在安装数据库环节一开始使用lnmp脚本进行数据库安装。各种规格和版本数据库全部安装过。在登录web界面配置Zabbix时候各种数据库连接不上的问题。例如：</p>
<blockquote>
<p>找不到目录。zabbix5 + mysql 5.7时出现这个情况</p>
</blockquote>
<p><img src="2021-02-03_173625.png"></p>
<blockquote>
<p>没有权限。zabbix5 + mysql 5.7时出现这个情况</p>
</blockquote>
<p><img src="2021-02-03_173708.png"></p>
<blockquote>
<p>无法连接数据库</p>
</blockquote>
<p><img src="2021-02-03_174040.png"></p>
<p>上述问题使用的组合为zabbix5 + centos7 + mysql 5.7 + nginx</p>
<h4 id="几个解决方法"><a href="#几个解决方法" class="headerlink" title="几个解决方法"></a>几个解决方法</h4><blockquote>
<p><code>最终改用5.5.68-MariaDB数据库并以yum安装的方式进行安装数据库。lnmp是以编译方式安装。</code>整体花费时间主要还是数据库这块，安装的方式不同导致的差异使安装Zabbix过程中踩坑不断。有时间的话再更详细的研究一下。</p>
</blockquote>
<blockquote>
<p>设置软连接(目录不存在则创建)</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir -p /var/lib/mysql</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -s /tmp/mysql.sock /var/lib/mysql/mysql.sock</span><br></pre></td></tr></table></figure>
<h4 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h4><ul>
<li><a href="https://bestmonitoringtools.com/how-to-install-zabbix-server-on-centos-or-rhel/">How to Install Zabbix 5.0 or 5.2 on CentOS 8 Step-by-Step</a></li>
<li><a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/6/html/security-enhanced_linux/sect-security-enhanced_linux-introduction-selinux_modes">SELINUX STATES AND MODES</a></li>
<li><a href="https://blog.csdn.net/weixin_44271499/article/details/110405250">zabbix部署与安装</a></li>
</ul>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Zabbix</tag>
      </tags>
  </entry>
  <entry>
    <title>macOS安装oh-my-zsh记录</title>
    <url>/2019/05/09/macOS%E5%AE%89%E8%A3%85oh-my-zsh%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="官网地址"><a href="#官网地址" class="headerlink" title="官网地址"></a>官网地址</h3><p><strong><a href="https://ohmyz.sh/">https://ohmyz.sh/</a></strong></p>
<h3 id="macOS安装方式"><a href="#macOS安装方式" class="headerlink" title="macOS安装方式"></a>macOS安装方式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ sh -c &quot;$(curl -fsSL https:&#x2F;&#x2F;raw.github.com&#x2F;robbyrussell&#x2F;oh-my-zsh&#x2F;master&#x2F;tools&#x2F;install.sh)&quot;</span><br></pre></td></tr></table></figure>
<h3 id="安装结果验证"><a href="#安装结果验证" class="headerlink" title="安装结果验证"></a>安装结果验证</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zsh --version</span><br></pre></td></tr></table></figure>
<blockquote>
<p>正确返回结果类似：zsh 5.1.1</p>
</blockquote>
<h3 id="设置zsh为默认shell"><a href="#设置zsh为默认shell" class="headerlink" title="设置zsh为默认shell"></a>设置zsh为默认shell</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chsh -s $(which zsh)</span><br></pre></td></tr></table></figure>
<h3 id="验证设置默认shell是否成功"><a href="#验证设置默认shell是否成功" class="headerlink" title="验证设置默认shell是否成功"></a>验证设置默认shell是否成功</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">echo $SHELL</span><br></pre></td></tr></table></figure>
<blockquote>
<p>正确返回结果类似：/bin/zsh</p>
</blockquote>
<h3 id="测试默认shell版本"><a href="#测试默认shell版本" class="headerlink" title="测试默认shell版本"></a>测试默认shell版本</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$SHELL --version</span><br></pre></td></tr></table></figure>
<blockquote>
<p>正确返回结果类似：zsh 5.1.1</p>
</blockquote>
<h3 id="配置主题"><a href="#配置主题" class="headerlink" title="配置主题"></a>配置主题</h3><blockquote>
<p>使用agnoster(<a href="https://github.com/agnoster/agnoster-zsh-theme">Official repository</a>)主题（macOS强烈建议使用<strong>iTerm 2 + Solarized Dark</strong>）</p>
</blockquote>
<h4 id="配置方式"><a href="#配置方式" class="headerlink" title="配置方式"></a>配置方式</h4><h5 id="打开"><a href="#打开" class="headerlink" title="打开"></a>打开</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi .zshrc</span><br></pre></td></tr></table></figure>
<h5 id="编辑"><a href="#编辑" class="headerlink" title="编辑"></a>编辑</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ZSH_THEME&#x3D;&quot;agnoster&quot;</span><br></pre></td></tr></table></figure>
<h3 id="主题需要搭配的字体及安装方式"><a href="#主题需要搭配的字体及安装方式" class="headerlink" title="主题需要搭配的字体及安装方式"></a>主题需要搭配的字体及安装方式</h3><blockquote>
<p>为了正确现实主题需要搭配该字体支持: <a href="https://github.com/Lokaltog/powerline-fonts">Powerline-patched font</a></p>
</blockquote>
<h4 id="安装方式"><a href="#安装方式" class="headerlink" title="安装方式"></a>安装方式</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;powerline&#x2F;fonts.git --depth&#x3D;1</span><br><span class="line">cd fonts</span><br><span class="line">.&#x2F;install.sh</span><br><span class="line">cd ..</span><br><span class="line">rm -rf fonts</span><br></pre></td></tr></table></figure>
<h3 id="终端字体设置"><a href="#终端字体设置" class="headerlink" title="终端字体设置"></a>终端字体设置</h3><blockquote>
<p>打开iTerm 2的设置Preferences，选择Profiles，选择Text，选择Change Font，选择Family=Meslo LG S for Powerline，Typeface=常规体，Size=13</p>
</blockquote>
]]></content>
      <categories>
        <category>macOS</category>
      </categories>
      <tags>
        <tag>shell</tag>
      </tags>
  </entry>
  <entry>
    <title>zabbix-agent的安装</title>
    <url>/2021/02/08/zabbix-agent%E7%9A%84%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<h3 id="在CentOS下安装"><a href="#在CentOS下安装" class="headerlink" title="在CentOS下安装"></a>在CentOS下安装</h3><blockquote>
<p>安装Zabbix仓库</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -Uvh https://repo.zabbix.com/zabbix/5.0/rhel/7/x86_64/zabbix-release-5.0-1.el7.noarch.rpm</span><br><span class="line">yum clean all</span><br></pre></td></tr></table></figure>
<blockquote>
<p>安装Zabbix代理</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install zabbix-agent</span><br></pre></td></tr></table></figure>
<blockquote>
<p>设置代理配置(<em>/etc/zabbix/zabbix_agentd.conf</em>)</p>
</blockquote>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="attr">LogFile</span>=<span class="string">/var/log/zabbix/zabbix_agentd.log</span></span><br><span class="line"><span class="attr">...</span></span><br><span class="line"></span><br><span class="line"><span class="attr">...</span></span><br><span class="line"><span class="attr">Server</span>=<span class="string">X.X.X.X</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>配置防火墙</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-port=10050/tcp --permanent</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Zabbix</tag>
      </tags>
  </entry>
  <entry>
    <title>如何在Docker中运行mysql</title>
    <url>/2019/05/07/%E5%A6%82%E4%BD%95%E5%9C%A8Docker%E4%B8%AD%E8%BF%90%E8%A1%8Cmysql/</url>
    <content><![CDATA[<h4 id="查看docker仓库中的mysql信息"><a href="#查看docker仓库中的mysql信息" class="headerlink" title="查看docker仓库中的mysql信息"></a>查看docker仓库中的mysql信息</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker search mysql</span><br></pre></td></tr></table></figure>
<h4 id="获取mysql镜像"><a href="#获取mysql镜像" class="headerlink" title="获取mysql镜像"></a>获取mysql镜像</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull mysql:5.7.23</span><br></pre></td></tr></table></figure>
<h4 id="启动mysql镜像"><a href="#启动mysql镜像" class="headerlink" title="启动mysql镜像"></a>启动mysql镜像</h4><h5 id="随容器自动启动"><a href="#随容器自动启动" class="headerlink" title="随容器自动启动"></a>随容器自动启动</h5><blockquote>
<p>方式一：通过映射目录方式挂载目录</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --restart&#x3D;always --name mysql \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;mysql&#x2F;0&#x2F;db:&#x2F;var&#x2F;lib&#x2F;mysql \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;mysql&#x2F;0&#x2F;conf:&#x2F;etc&#x2F;mysql&#x2F;conf.d \</span><br><span class="line">-e MYSQL_ROOT_PASSWORD&#x3D;123456 -d -p 10.12.64.90:3306:3306 mysql:5.7.23</span><br></pre></td></tr></table></figure>
<blockquote>
<p>方式二：通过卷的方式挂载目录</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --restart&#x3D;always --name mysql \</span><br><span class="line">-v mysql_db_v1:&#x2F;var&#x2F;lib&#x2F;mysql \</span><br><span class="line">-v mysql_conf_v1:&#x2F;etc&#x2F;mysql&#x2F;conf.d \</span><br><span class="line">-e MYSQL_ROOT_PASSWORD&#x3D;123456 -d -p 3306:3306 mysql:5.7.23</span><br></pre></td></tr></table></figure>
<blockquote>
<p>备注：<code>--restart=always</code>可以替换成<code>--rm</code>表示容器停止后不保留历史记录(2者不能同时使用)。<code>mysql_data_v1</code>用于装载mysql数据，<code>mysql_conf_v1</code>用于装载mysql的配置。如果卷不存在docker会自动创建。</p>
</blockquote>
<h4 id="自定义配置文件"><a href="#自定义配置文件" class="headerlink" title="自定义配置文件"></a>自定义配置文件</h4><blockquote>
<p>mysql使用utf8mb4方式配置:<code>/data/docker/instance/mysql/0/conf</code>为自定义配置的目录，该目录下的cnf文件将随容器启动时合并至mysql中</p>
</blockquote>
<h5 id="编辑文件"><a href="#编辑文件" class="headerlink" title="编辑文件"></a>编辑文件</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi my.cnf</span><br></pre></td></tr></table></figure>
<h5 id="修改内容"><a href="#修改内容" class="headerlink" title="修改内容"></a>修改内容</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[client]</span><br><span class="line">default-character-set&#x3D;utf8mb4</span><br><span class="line"></span><br><span class="line">[mysql]</span><br><span class="line">default-character-set&#x3D;utf8mb4</span><br><span class="line"></span><br><span class="line">[mysqld]</span><br><span class="line">sql_mode&#x3D;ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION</span><br><span class="line">lower_case_table_names &#x3D; 1</span><br><span class="line">character-set-client-handshake&#x3D;FALSE</span><br><span class="line">character-set-server&#x3D;utf8mb4</span><br><span class="line">collation-server&#x3D;utf8mb4_unicode_ci</span><br><span class="line">init_connect&#x3D;&#39;SET NAMES utf8mb4&#39;</span><br><span class="line">skip-host-cache</span><br><span class="line">skip-name-resolve</span><br></pre></td></tr></table></figure>
<h4 id="重启容易使配置生效"><a href="#重启容易使配置生效" class="headerlink" title="重启容易使配置生效"></a>重启容易使配置生效</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker restart mysql</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>如何在Docker中运行nginx</title>
    <url>/2019/07/18/%E5%A6%82%E4%BD%95%E5%9C%A8Docker%E4%B8%AD%E8%BF%90%E8%A1%8Cnginx/</url>
    <content><![CDATA[<h3 id="从私库镜像创建容器"><a href="#从私库镜像创建容器" class="headerlink" title="从私库镜像创建容器"></a>从私库镜像创建容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run \</span><br><span class="line">-d \</span><br><span class="line">-p 192.168.37.130:80:80 \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;nginx&#x2F;0&#x2F;wwwroot:&#x2F;usr&#x2F;share&#x2F;nginx&#x2F;html \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;nginx&#x2F;0&#x2F;nginx:&#x2F;etc&#x2F;nginx \</span><br><span class="line">--restart&#x3D;always \</span><br><span class="line">--name nginx \</span><br><span class="line">registry:5000&#x2F;nginx:1.16.1</span><br></pre></td></tr></table></figure>
<h3 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h3><ol>
<li><p>nginx页面目录映射至本地<strong>wwwroot</strong>目录</p>
</li>
<li><p>nginx配置目录映射至本地<strong>nginx</strong>目录</p>
</li>
<li><p>可将nginx容器中的配置**/etc/nginx<strong>目录下内容复制到本地</strong>/data/docker/instance/nginx/0/nginx**目录下，以备做定制化修改，参考操作如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --name tmp-nginx-container -d nginx:1.16.1</span><br><span class="line">docker cp tmp-nginx-container:&#x2F;etc&#x2F;nginx&#x2F; &#x2F;data&#x2F;docker&#x2F;instance&#x2F;nginx&#x2F;0&#x2F;</span><br><span class="line">docker rm -f tmp-nginx-container</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
        <tag>nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>记录卸载macOS下mysql步骤</title>
    <url>/2020/03/21/%E8%AE%B0%E5%BD%95%E5%8D%B8%E8%BD%BDmacOS%E4%B8%8Bmysql%E6%AD%A5%E9%AA%A4/</url>
    <content><![CDATA[<blockquote>
<p>在终端下执行如下命令（卸载mysql基于默认安装目录）</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo rm /usr/<span class="built_in">local</span>/mysql</span><br><span class="line">sudo rm -rf /usr/<span class="built_in">local</span>/mysql*</span><br><span class="line">sudo rm -rf /Library/StartupItems/MySQLCOM</span><br><span class="line">sudo rm -rf /Library/PreferencePanes/My*</span><br><span class="line">rm -rf ~/Library/PreferencePanes/My*</span><br><span class="line">sudo rm -rf /Library/Receipts/mysql*</span><br><span class="line">sudo rm -rf /Library/Receipts/MySQL*</span><br><span class="line">sudo rm -rf /var/db/receipts/com.mysql.*</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>macOS</category>
      </categories>
      <tags>
        <tag>shell</tag>
      </tags>
  </entry>
  <entry>
    <title>感觉好用的IntelliJ IDEA插件列表</title>
    <url>/2019/05/14/%E6%84%9F%E8%A7%89%E5%A5%BD%E7%94%A8%E7%9A%84IntelliJ-IDEA%E6%8F%92%E4%BB%B6%E5%88%97%E8%A1%A8/</url>
    <content><![CDATA[<h3 id="插件列表"><a href="#插件列表" class="headerlink" title="插件列表"></a>插件列表</h3><ul>
<li><h4 id="ignore"><a href="#ignore" class="headerlink" title=".ignore"></a>.ignore</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/7495--ignore">plugins.jetbrains.com</a> <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://ignore.hsz.mobi/">website</a> <i class="fa fa-hashtag" aria-hidden="true"></i><a href="https://github.com/hsz/idea-gitignore">github</a></p>
<blockquote>
<p>.ignore is a plugin for .gitignore (Git), .hgignore (Mercurial), .npmignore (NPM), .dockerignore (Docker), .chefignore (Chef), .cvsignore (CVS), .bzrignore (Bazaar), .boringignore (Darcs), .mtn-ignore (Monotone), ignore-glob (Fossil), .jshintignore (JSHint), .tfignore (Team Foundation), .p4ignore (Perforce), .prettierignore (Prettier), .flooignore (Floobits), .eslintignore (ESLint), .cfignore (Cloud Foundry), .jpmignore (Jetpack), .stylelintignore (StyleLint), .stylintignore (Stylint), .swagger-codegen-ignore (Swagger Codegen), .helmignore (Kubernetes Helm), .upignore (Up), .prettierignore (Prettier), .ebignore (ElasticBeanstalk) files in your project.</p>
</blockquote>
</li>
<li><h4 id="Alibaba-Java-Coding-Guidelines"><a href="#Alibaba-Java-Coding-Guidelines" class="headerlink" title="Alibaba Java Coding Guidelines"></a>Alibaba Java Coding Guidelines</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/10046-alibaba-java-coding-guidelines">plugins.jetbrains.com</a> <i class="fa fa-hashtag" aria-hidden="true"></i><a href="https://github.com/alibaba/p3c">github</a></p>
<blockquote>
<p>Alibaba Java Coding Guidelines plugin support.</p>
</blockquote>
</li>
<li><h4 id="CodeGlance"><a href="#CodeGlance" class="headerlink" title="CodeGlance"></a>CodeGlance</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/7275-codeglance">plugins.jetbrains.com</a></p>
<blockquote>
<p>Embeds a code minimap similar to the one found in Sublime into the editor pane. Works with both light and dark themes using your customized colors for syntax highlighting.</p>
</blockquote>
</li>
<li><h4 id="Grep-Console"><a href="#Grep-Console" class="headerlink" title="Grep Console"></a>Grep Console</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/7125-grep-console">plugins.jetbrains.com</a></p>
<blockquote>
<p>Grep, tail, filter, highlight… everything you need for a console. Also can highlight the editor - nice for analyzing logs… Features:<br>Change colors of matching text.<br>Grep output into a new console tab.<br>Change output or execute any action using custom groovy scripts or plugins.<br>Filter out unwanted lines.<br>Fold output.<br>Play sounds on matches.<br>Clear Console on matches.<br>Tail files*.</p>
</blockquote>
</li>
<li><h4 id="iBATIS-MyBatis-mini-plugin"><a href="#iBATIS-MyBatis-mini-plugin" class="headerlink" title="iBATIS/MyBatis mini-plugin"></a>iBATIS/MyBatis mini-plugin</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/6725-ibatis-mybatis-mini-plugin">plugins.jetbrains.com</a></p>
<blockquote>
<p>Simple iBATIS/MyBatis plugin<br>Contains essential features only, no datasource integration (yet)<br>Works for IDEA &gt;= 10.0.3</p>
</blockquote>
</li>
<li><h4 id="Maven-Helper"><a href="#Maven-Helper" class="headerlink" title="Maven Helper"></a>Maven Helper</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/7179-maven-helper">plugins.jetbrains.com</a></p>
<blockquote>
<p>A must have plugin for working with Maven.  Provides:<br>easy way for finding and excluding conflicting dependencies<br>actions to run/debug maven goals for a module that contains the current file or on the root module<br>actions to run/debug the current test file. If maven-surefire-plugin is configured to skip or exclude the test, ‘verify’ goal will be used, see different configuration styles at <a href="https://github.com/harishkannarao/MavenSurefireFailsafeExamples">https://github.com/harishkannarao/MavenSurefireFailsafeExamples</a><br>Usage:<br>Right click in Editor | Run Maven<br>Right click in Project View Toolbar | (Run|Debug) Maven<br>CTRL + ALT + R - “Quick Run Maven Goal” action (you can use Delete key in the popup)<br>CTRL + SHIFT + ALT + R - for running goals on the root module (you can use Delete key in the popup)<br>Customize goals: Settings | Other Settings | Maven Helper<br>Define shortcuts: Settings | Keymap | Plug-ins | Maven Helper<br>Open pom file, click on ‘Dependency Analyzer’ tab, right click in the tree for context actions.</p>
</blockquote>
</li>
<li><h4 id="RestfulToolkit"><a href="#RestfulToolkit" class="headerlink" title="RestfulToolkit"></a>RestfulToolkit</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/10292-restfultoolkit">plugins.jetbrains.com</a></p>
<blockquote>
<p>A toolkit for RESTful services development.</p>
</blockquote>
</li>
</ul>
<p>1.ONE STEP to navigate to service declaration. ( use: Ctrl(Command) + \ or Ctrl + Alt + N )<br>2.Show RESTful services structure.<br>3.A simple http request tool.<br>4.Add some useful functions at request method, Generate&amp;Copy Query Param, Generate&amp;Copy URL, etc.<br>5.Other functions: Convert to JSON; to java class，format json data ( Windows: Ctrl + Enter; Mac: Command + Enter ) .<br>support Spring framework (Spring MVC / Spring Boot 1.x,2.x)<br>support JAX-RS<br>support Java and Kotlin languages</p>
<ul>
<li><h4 id="SonarLint"><a href="#SonarLint" class="headerlink" title="SonarLint"></a>SonarLint</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/7973-sonarlint">plugins.jetbrains.com</a></p>
<blockquote>
<p>SonarLint is an IDE extension that helps you detect and fix quality issues as you write code. Like a spell checker, SonarLint squiggles flaws so they can be fixed before committing code. You can get it directly from the IntelliJ IDEA Plugin Repository, and it will then detect new bugs and quality issues as you code (Java, Kotlin, Ruby, JavaScript, PHP and Python).<br>If your project is analyzed on SonarQube or on SonarCloud, SonarLint can connect to the server to retrieve the appropriate quality profiles and settings for that project. Java 8 is required to run SonarLint.</p>
</blockquote>
</li>
<li><h4 id="Statistic"><a href="#Statistic" class="headerlink" title="Statistic"></a>Statistic</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="http://plugins.jetbrains.com/plugin/4509-statistic">plugins.jetbrains.com</a></p>
<blockquote>
<p>Shows project statistic. This plugin shows files sorted by their extension along with size, line count LOC etc. User can select (Project/Module/Package/File) scope using the ‘Refresh on selection’ button. (This plugin requires Java 1.8)</p>
</blockquote>
</li>
<li><h4 id="Jenkins-Control-Plugin"><a href="#Jenkins-Control-Plugin" class="headerlink" title="Jenkins Control Plugin"></a>Jenkins Control Plugin</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="https://plugins.jetbrains.com/plugin/6110-jenkins-control-plugin/">plugins.jetbrains.com</a></p>
<blockquote>
<p>A Jenkins Plugin for Intellij</p>
</blockquote>
</li>
<li><h4 id="String-Manipulation"><a href="#String-Manipulation" class="headerlink" title="String Manipulation"></a>String Manipulation</h4><p>  <i class="fa fa-hashtag" aria-hidden="true"></i><a href="https://plugins.jetbrains.com/plugin/2162-string-manipulation/">plugins.jetbrains.com</a></p>
<blockquote>
<p>Switch case (camelCase, kebab-lowercase, KEBAB-UPPERCASE, snake_case, SCREAMING_SNAKE_CASE, dot.case, words lowercase, First word capitalized, Words Capitalized, PascalCase)…</p>
</blockquote>
</li>
</ul>
]]></content>
      <categories>
        <category>IDE</category>
      </categories>
      <tags>
        <tag>IntelliJ IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>设置VMware Workstation虚拟机自动启动方式</title>
    <url>/2019/05/13/%E8%AE%BE%E7%BD%AEVMware-Workstation%E8%99%9A%E6%8B%9F%E6%9C%BA%E8%87%AA%E5%8A%A8%E5%90%AF%E5%8A%A8%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="环境和版本"><a href="#环境和版本" class="headerlink" title="环境和版本"></a>环境和版本</h3><ul>
<li><p>Microsoft Windows 10.0.17134 Build 17134</p>
</li>
<li><p>VMware® Workstation 15 Pro</p>
</li>
<li><p>15.0.0 build-10134415</p>
</li>
</ul>
<h3 id="获取虚拟机配置文件路径"><a href="#获取虚拟机配置文件路径" class="headerlink" title="获取虚拟机配置文件路径"></a>获取虚拟机配置文件路径</h3><ol>
<li>右键虚拟机<strong>设置</strong></li>
<li>复制<strong>选项</strong>页<strong>高级</strong>右侧<strong>配置</strong>中的路径</li>
</ol>
<h3 id="设置自动启动"><a href="#设置自动启动" class="headerlink" title="设置自动启动"></a>设置自动启动</h3><ol>
<li><code>Win+R</code>输入<code>shell:startup</code></li>
<li>新建<strong>快捷方式</strong>，键入对象的位置<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;D:\Users\admin\Documents\Virtual Machines\CentOS7_64_1611_B\CentOS7_64_1611_B.vmx&quot; nogui</span><br></pre></td></tr></table></figure></li>
<li>键入快捷方式的名称，点击完成</li>
</ol>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>VMware</tag>
      </tags>
  </entry>
  <entry>
    <title>运行Docker容器后提示WARNING: IPv4 forwarding is disabled. Networking will not work.的解决方法</title>
    <url>/2019/10/06/%E8%BF%90%E8%A1%8CDocker%E5%AE%B9%E5%99%A8%E5%90%8E%E6%8F%90%E7%A4%BAWARNING-IPv4-forwarding-is-disabled-Networking-will-not-work-%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<blockquote>
<p>打开**/usr/lib/sysctl.d/00-system.conf<strong>添加</strong>net.ipv4.ip_forward = 1**后重启网络服务</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Kernel sysctl configuration file</span><br><span class="line">#</span><br><span class="line"># For binary values, 0 is disabled, 1 is enabled.  See sysctl(8) and</span><br><span class="line"># sysctl.conf(5) for more details.</span><br><span class="line"></span><br><span class="line"># Disable netfilter on bridges.</span><br><span class="line">net.ipv4.ip_forward &#x3D; 1</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables &#x3D; 0</span><br><span class="line">net.bridge.bridge-nf-call-iptables &#x3D; 0</span><br><span class="line">net.bridge.bridge-nf-call-arptables &#x3D; 0</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>通过Docker方式安装dnsmasq</title>
    <url>/2021/01/29/%E9%80%9A%E8%BF%87Docker%E6%96%B9%E5%BC%8F%E5%AE%89%E8%A3%85dnsmasq/</url>
    <content><![CDATA[<h3 id="创建容器"><a href="#创建容器" class="headerlink" title="创建容器"></a>创建容器</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d\</span><br><span class="line">    --name dnsmasq_0 \</span><br><span class="line">    --restart always \</span><br><span class="line">    -p 53:53/udp \</span><br><span class="line">    -p 8053:8080 \</span><br><span class="line">    -v /data/docker/instance/dnsmasq/0/dnsmasq.conf:/etc/dnsmasq.conf \</span><br><span class="line">    --log-opt <span class="string">&quot;max-size=100m&quot;</span> \</span><br><span class="line">    -e <span class="string">&quot;HTTP_USER=user&quot;</span> \</span><br><span class="line">    -e <span class="string">&quot;HTTP_PASS=password&quot;</span> \</span><br><span class="line">    jpillora/dnsmasq</span><br></pre></td></tr></table></figure>
<blockquote>
<p><strong>备注：</strong></p>
<p>在安装成功后，运行了一段时间发现DNS服务无法正常提供，后研究发现DNS服务所在主机无法PING访问外网，提示：connect: Network is unreachable，经研究查阅资料后解决：（配置CentOS网卡信息）</p>
<p>NM_CONTROLLED=no</p>
</blockquote>
<h3 id="自定义配置文件dnsmasq-conf"><a href="#自定义配置文件dnsmasq-conf" class="headerlink" title="自定义配置文件dnsmasq.conf"></a>自定义配置文件<em>dnsmasq.conf</em></h3><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">#dnsmasq config, for a complete example, see:</span></span><br><span class="line"><span class="comment">#  http://oss.segetech.com/intra/srv/dnsmasq.conf</span></span><br><span class="line"><span class="comment">#log all dns queries</span></span><br><span class="line"><span class="attr">log-queries</span></span><br><span class="line"><span class="comment">#dont use hosts nameservers</span></span><br><span class="line"><span class="attr">no-resolv</span></span><br><span class="line"><span class="comment">#use cloudflare as default nameservers, prefer 1^4</span></span><br><span class="line"><span class="attr">server</span>=<span class="string">114.114.114.114</span></span><br><span class="line"><span class="attr">server</span>=<span class="string">223.6.6.6</span></span><br><span class="line"><span class="attr">strict-order</span></span><br><span class="line"><span class="comment">#serve all .company queries using a specific nameserver</span></span><br><span class="line"><span class="attr">server</span>=<span class="string">/company/10.0.0.1</span></span><br><span class="line"><span class="comment">#explicitly define host-ip mappings</span></span><br><span class="line"><span class="attr">address</span>=<span class="string">/myhost.company/10.0.0.2</span></span><br><span class="line"><span class="attr">address</span>=<span class="string">/a.abc.ef/10.1.0.1</span></span><br><span class="line"><span class="attr">address</span>=<span class="string">/b.abc.ef/10.2.0.2</span></span><br></pre></td></tr></table></figure>
<h3 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h3><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># /etc/dnsmasq.conf (0644): server dnsmasq configuration</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Configuration file for dnsmasq.</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Format is one option per line, legal options are the same</span></span><br><span class="line"><span class="comment"># as the long options legal on the command line. See</span></span><br><span class="line"><span class="comment"># &quot;/usr/sbin/dnsmasq --help&quot; or &quot;man 8 dnsmasq&quot; for details.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The following two options make you a better netizen, since they</span></span><br><span class="line"><span class="comment"># tell dnsmasq to filter out queries which the public DNS cannot</span></span><br><span class="line"><span class="comment"># answer, and which load the servers (especially the root servers)</span></span><br><span class="line"><span class="comment"># uneccessarily. If you have a dial-on-demand link they also stop</span></span><br><span class="line"><span class="comment"># these requests from bringing up the link uneccessarily.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Never forward plain names (without a dot or domain part)</span></span><br><span class="line"><span class="attr">domain-needed</span></span><br><span class="line"><span class="comment"># Never forward addresses in the non-routed address spaces.</span></span><br><span class="line"><span class="attr">bogus-priv</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment this to filter useless windows-originated DNS requests</span></span><br><span class="line"><span class="comment"># which can trigger dial-on-demand links needlessly.</span></span><br><span class="line"><span class="comment"># Note that (amongst other things) this blocks all SRV requests,</span></span><br><span class="line"><span class="comment"># so don&#x27;t use it if you use eg Kerberos.</span></span><br><span class="line"><span class="comment"># This option only affects forwarding, SRV records originating for</span></span><br><span class="line"><span class="comment"># dnsmasq (via srv-host= lines) are not suppressed by it.</span></span><br><span class="line"><span class="comment">#filterwin2k</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Change this line if you want dns to get its upstream servers from</span></span><br><span class="line"><span class="comment"># somewhere other that /etc/resolv.conf</span></span><br><span class="line"><span class="comment">#resolv-file=</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># By  default,  dnsmasq  will  send queries to any of the upstream</span></span><br><span class="line"><span class="comment"># servers it knows about and tries to favour servers to are  known</span></span><br><span class="line"><span class="comment"># to  be  up.  Uncommenting this forces dnsmasq to try each query</span></span><br><span class="line"><span class="comment"># with  each  server  strictly  in  the  order  they   appear   in</span></span><br><span class="line"><span class="comment"># /etc/resolv.conf</span></span><br><span class="line"><span class="comment">#strict-order</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you don&#x27;t want dnsmasq to read /etc/resolv.conf or any other</span></span><br><span class="line"><span class="comment"># file, getting its servers from this file instead (see below), then</span></span><br><span class="line"><span class="comment"># uncomment this.</span></span><br><span class="line"><span class="comment">#no-resolv</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you don&#x27;t want dnsmasq to poll /etc/resolv.conf or other resolv</span></span><br><span class="line"><span class="comment"># files for changes and re-read them then uncomment this.</span></span><br><span class="line"><span class="comment">#no-poll</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Add other name servers here, with domain specs if they are for</span></span><br><span class="line"><span class="comment"># non-public domains.</span></span><br><span class="line"><span class="comment">#server=/localnet/192.168.0.1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Example of routing PTR queries to nameservers: this will send all </span></span><br><span class="line"><span class="comment"># address-&gt;name queries for 192.168.3/24 to nameserver 10.1.2.3</span></span><br><span class="line"><span class="comment">#server=/3.168.192.in-addr.arpa/10.1.2.3</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Add local-only domains here, queries in these domains are answered</span></span><br><span class="line"><span class="comment"># from /etc/hosts or DHCP only.</span></span><br><span class="line"><span class="comment">#local=/localnet/</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Add domains which you want to force to an IP address here.</span></span><br><span class="line"><span class="comment"># The example below send any host in doubleclick.net to a local</span></span><br><span class="line"><span class="comment"># webserver.</span></span><br><span class="line"><span class="comment">#address=/doubleclick.net/127.0.0.1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you want dnsmasq to change uid and gid to something other</span></span><br><span class="line"><span class="comment"># than the default, edit the following lines.</span></span><br><span class="line"><span class="comment">#user=</span></span><br><span class="line"><span class="comment">#group=</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you want dnsmasq to listen for DHCP and DNS requests only on</span></span><br><span class="line"><span class="comment"># specified interfaces (and the loopback) give the name of the</span></span><br><span class="line"><span class="comment"># interface (eg eth0) here.</span></span><br><span class="line"><span class="comment"># Repeat the line for more than one interface.</span></span><br><span class="line"><span class="attr">interface</span>=<span class="string">eth1</span></span><br><span class="line"><span class="comment"># Or you can specify which interface _not_ to listen on</span></span><br><span class="line"><span class="comment">#except-interface=</span></span><br><span class="line"><span class="comment"># Or which to listen on by address (remember to include 127.0.0.1 if</span></span><br><span class="line"><span class="comment"># you use this.)</span></span><br><span class="line"><span class="comment">#listen-address=</span></span><br><span class="line"><span class="comment"># If you want dnsmasq to provide only DNS service on an interface,</span></span><br><span class="line"><span class="comment"># configure it as shown above, and then use the following line to</span></span><br><span class="line"><span class="comment"># disable DHCP on it.</span></span><br><span class="line"><span class="comment">#no-dhcp-interface=</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># On systems which support it, dnsmasq binds the wildcard address,</span></span><br><span class="line"><span class="comment"># even when it is listening on only some interfaces. It then discards</span></span><br><span class="line"><span class="comment"># requests that it shouldn&#x27;t reply to. This has the advantage of</span></span><br><span class="line"><span class="comment"># working even when interfaces come and go and change address. If you</span></span><br><span class="line"><span class="comment"># want dnsmasq to really bind only the interfaces it is listening on,</span></span><br><span class="line"><span class="comment"># uncomment this option. About the only time you may need this is when</span></span><br><span class="line"><span class="comment"># running another nameserver on the same machine.</span></span><br><span class="line"><span class="comment">#bind-interfaces</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you don&#x27;t want dnsmasq to read /etc/hosts, uncomment the</span></span><br><span class="line"><span class="comment"># following line.</span></span><br><span class="line"><span class="comment">#no-hosts</span></span><br><span class="line"><span class="comment"># or if you want it to read another file, as well as /etc/hosts, use</span></span><br><span class="line"><span class="comment"># this.</span></span><br><span class="line"><span class="comment">#addn-hosts=/etc/banner_add_hosts</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set this (and domain: see below) if you want to have a domain</span></span><br><span class="line"><span class="comment"># automatically added to simple names in a hosts-file.</span></span><br><span class="line"><span class="comment">#expand-hosts</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the domain for dnsmasq. this is optional, but if it is set, it</span></span><br><span class="line"><span class="comment"># does the following things.</span></span><br><span class="line"><span class="comment"># 1) Allows DHCP hosts to have fully qualified domain names, as long</span></span><br><span class="line"><span class="comment">#     as the domain part matches this setting.</span></span><br><span class="line"><span class="comment"># 2) Sets the &quot;domain&quot; DHCP option thereby potentially setting the</span></span><br><span class="line"><span class="comment">#    domain of all systems configured by DHCP</span></span><br><span class="line"><span class="comment"># 3) Provides the domain part for &quot;expand-hosts&quot;</span></span><br><span class="line"><span class="comment">#domain=thekelleys.org.uk</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment this to enable the integrated DHCP server, you need</span></span><br><span class="line"><span class="comment"># to supply the range of addresses available for lease and optionally</span></span><br><span class="line"><span class="comment"># a lease time. If you have more than one network, you will need to</span></span><br><span class="line"><span class="comment"># repeat this for each network on which you want to supply DHCP</span></span><br><span class="line"><span class="comment"># service.</span></span><br><span class="line"><span class="comment">#dhcp-range=192.168.0.50,192.168.0.150,12h</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># This is an example of a DHCP range where the netmask is given. This</span></span><br><span class="line"><span class="comment"># is needed for networks we reach the dnsmasq DHCP server via a relay</span></span><br><span class="line"><span class="comment"># agent. If you don&#x27;t know what a DHCP relay agent is, you probably</span></span><br><span class="line"><span class="comment"># don&#x27;t need to worry about this.</span></span><br><span class="line"><span class="comment">#dhcp-range=192.168.0.50,192.168.0.150,255.255.255.0,12h</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># This is an example of a DHCP range with a network-id, so that</span></span><br><span class="line"><span class="comment"># some DHCP options may be set only for this network.</span></span><br><span class="line"><span class="comment">#dhcp-range=red,192.168.0.50,192.168.0.150</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Supply parameters for specified hosts using DHCP. There are lots</span></span><br><span class="line"><span class="comment"># of valid alternatives, so we will give examples of each. Note that</span></span><br><span class="line"><span class="comment"># IP addresses DO NOT have to be in the range given above, they just</span></span><br><span class="line"><span class="comment"># need to be on the same network. The order of the parameters in these</span></span><br><span class="line"><span class="comment"># do not matter, it&#x27;s permissble to give name,adddress and MAC in any order</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Always allocate the host with ethernet address 11:22:33:44:55:66</span></span><br><span class="line"><span class="comment"># The IP address 192.168.0.60</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,192.168.0.60</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Always set the name of the host with hardware address</span></span><br><span class="line"><span class="comment"># 11:22:33:44:55:66 to be &quot;fred&quot;</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,fred</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Always give the host with ethernet address 11:22:33:44:55:66</span></span><br><span class="line"><span class="comment"># the name fred and IP address 192.168.0.60 and lease time 45 minutes</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,fred,192.168.0.60,45m</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Give the machine which says it&#x27;s name is &quot;bert&quot; IP address</span></span><br><span class="line"><span class="comment"># 192.168.0.70 and an infinite lease</span></span><br><span class="line"><span class="comment">#dhcp-host=bert,192.168.0.70,infinite</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Always give the host with client identifier 01:02:02:04</span></span><br><span class="line"><span class="comment"># the IP address 192.168.0.60</span></span><br><span class="line"><span class="comment">#dhcp-host=id:01:02:02:04,192.168.0.60</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Always give the host with client identifier &quot;marjorie&quot;</span></span><br><span class="line"><span class="comment"># the IP address 192.168.0.60</span></span><br><span class="line"><span class="comment">#dhcp-host=id:marjorie,192.168.0.60</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Enable the address given for &quot;judge&quot; in /etc/hosts</span></span><br><span class="line"><span class="comment"># to be given to a machine presenting the name &quot;judge&quot; when</span></span><br><span class="line"><span class="comment"># it asks for a DHCP lease.</span></span><br><span class="line"><span class="comment">#dhcp-host=judge</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Never offer DHCP service to a machine whose ethernet</span></span><br><span class="line"><span class="comment"># address is 11:22:33:44:55:66</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,ignore</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Ignore any client-id presented by the machine with ethernet</span></span><br><span class="line"><span class="comment"># address 11:22:33:44:55:66. This is useful to prevent a machine</span></span><br><span class="line"><span class="comment"># being treated differently when running under different OS&#x27;s or</span></span><br><span class="line"><span class="comment"># between PXE boot and OS boot.</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,id:*</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send extra options which are tagged as &quot;red&quot; to</span></span><br><span class="line"><span class="comment"># the machine with ethernet address 11:22:33:44:55:66</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:44:55:66,net:red</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send extra options which are tagged as &quot;red&quot; to</span></span><br><span class="line"><span class="comment"># any machine with ethernet address starting 11:22:33:</span></span><br><span class="line"><span class="comment">#dhcp-host=11:22:33:*:*:*,net:red</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Ignore any clients which are specified in dhcp-host lines</span></span><br><span class="line"><span class="comment"># or /etc/ethers. Equivalent to ISC &quot;deny unkown-clients&quot;.</span></span><br><span class="line"><span class="comment"># This relies on the special &quot;known&quot; tag which is set when </span></span><br><span class="line"><span class="comment"># a host is matched.</span></span><br><span class="line"><span class="comment">#dhcp-ignore=#known</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send extra options which are tagged as &quot;red&quot; to any machine whose</span></span><br><span class="line"><span class="comment"># DHCP vendorclass string includes the substring &quot;Linux&quot;</span></span><br><span class="line"><span class="comment">#dhcp-vendorclass=red,Linux</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send extra options which are tagged as &quot;red&quot; to any machine one</span></span><br><span class="line"><span class="comment"># of whose DHCP userclass strings includes the substring &quot;accounts&quot;</span></span><br><span class="line"><span class="comment">#dhcp-userclass=red,accounts</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send extra options which are tagged as &quot;red&quot; to any machine whose</span></span><br><span class="line"><span class="comment"># MAC address matches the pattern.</span></span><br><span class="line"><span class="comment">#dhcp-mac=red,00:60:8C:*:*:*</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If this line is uncommented, dnsmasq will read /etc/ethers and act</span></span><br><span class="line"><span class="comment"># on the ethernet-address/IP pairs found there just as if they had</span></span><br><span class="line"><span class="comment"># been given as --dhcp-host options. Useful if you keep</span></span><br><span class="line"><span class="comment"># MAC-address/host mappings there for other purposes.</span></span><br><span class="line"><span class="comment">#read-ethers</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send options to hosts which ask for a DHCP lease.</span></span><br><span class="line"><span class="comment"># See RFC 2132 for details of available options.</span></span><br><span class="line"><span class="comment"># Common options can be given to dnsmasq by name: </span></span><br><span class="line"><span class="comment"># run &quot;dnsmasq --help dhcp&quot; to get a list.</span></span><br><span class="line"><span class="comment"># Note that all the common settings, such as netmask and</span></span><br><span class="line"><span class="comment"># broadcast address, DNS server and default route, are given</span></span><br><span class="line"><span class="comment"># sane defaults by dnsmasq. You very likely will not need </span></span><br><span class="line"><span class="comment"># any dhcp-options. If you use Windows clients and Samba, there</span></span><br><span class="line"><span class="comment"># are some options which are recommended, they are detailed at the</span></span><br><span class="line"><span class="comment"># end of this section.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Override the default route supplied by dnsmasq, which assumes the</span></span><br><span class="line"><span class="comment"># router is the same machine as the one running dnsmasq.</span></span><br><span class="line"><span class="comment">#dhcp-option=3,1.2.3.4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Do the same thing, but using the option name</span></span><br><span class="line"><span class="comment">#dhcp-option=option:router,1.2.3.4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Override the default route supplied by dnsmasq and send no default</span></span><br><span class="line"><span class="comment"># route at all. Note that this only works for the options sent by</span></span><br><span class="line"><span class="comment"># default (1, 3, 6, 12, 28) the same line will send a zero-length option </span></span><br><span class="line"><span class="comment"># for all other option numbers.</span></span><br><span class="line"><span class="comment">#dhcp-option=3</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the NTP time server addresses to 192.168.0.4 and 10.10.0.5</span></span><br><span class="line"><span class="comment">#dhcp-option=option:ntp-server,192.168.0.4,10.10.0.5</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the NTP time server address to be the same machine as</span></span><br><span class="line"><span class="comment"># is running dnsmasq</span></span><br><span class="line"><span class="comment">#dhcp-option=42,0.0.0.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the NIS domain name to &quot;welly&quot;</span></span><br><span class="line"><span class="comment">#dhcp-option=40,welly</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the default time-to-live to 50</span></span><br><span class="line"><span class="comment">#dhcp-option=23,50</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the &quot;all subnets are local&quot; flag</span></span><br><span class="line"><span class="comment">#dhcp-option=27,1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send the etherboot magic flag and then etherboot options (a string).</span></span><br><span class="line"><span class="comment">#dhcp-option=128,e4:45:74:68:00:00</span></span><br><span class="line"><span class="comment">#dhcp-option=129,NIC=eepro100</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Specify an option which will only be sent to the &quot;red&quot; network</span></span><br><span class="line"><span class="comment"># (see dhcp-range for the declaration of the &quot;red&quot; network)</span></span><br><span class="line"><span class="comment"># Note that the net: part must precede the option: part.</span></span><br><span class="line"><span class="comment">#dhcp-option = net:red, option:ntp-server, 192.168.1.1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The following DHCP options set up dnsmasq in the same way as is specified</span></span><br><span class="line"><span class="comment"># for the ISC dhcpcd in</span></span><br><span class="line"><span class="comment"># http://www.samba.org/samba/ftp/docs/textdocs/DHCP-Server-Configuration.txt</span></span><br><span class="line"><span class="comment"># adapted for a typical dnsmasq installation where the host running</span></span><br><span class="line"><span class="comment"># dnsmasq is also the host running samba.</span></span><br><span class="line"><span class="comment"># you may want to uncomment them if you use Windows clients and Samba.</span></span><br><span class="line"><span class="comment">#dhcp-option=19,0           # option ip-forwarding off</span></span><br><span class="line"><span class="comment">#dhcp-option=44,0.0.0.0     # set netbios-over-TCP/IP nameserver(s) aka WINS server(s)</span></span><br><span class="line"><span class="comment">#dhcp-option=45,0.0.0.0     # netbios datagram distribution server</span></span><br><span class="line"><span class="comment">#dhcp-option=46,8           # netbios node type</span></span><br><span class="line"><span class="comment">#dhcp-option=47             # empty netbios scope.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send RFC-3397 DNS domain search DHCP option. WARNING: Your DHCP client</span></span><br><span class="line"><span class="comment"># probably doesn&#x27;t support this......</span></span><br><span class="line"><span class="comment">#dhcp-option=option:domain-search,eng.apple.com,marketing.apple.com</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send RFC-3442 classless static routes (note the netmask encoding)</span></span><br><span class="line"><span class="comment">#dhcp-option=121,192.168.1.0/24,1.2.3.4,10.0.0.0/8,5.6.7.8</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send vendor-class specific options encapsulated in DHCP option 43. </span></span><br><span class="line"><span class="comment"># The meaning of the options is defined by the vendor-class so</span></span><br><span class="line"><span class="comment"># options are sent only when the client supplied vendor class</span></span><br><span class="line"><span class="comment"># matches the class given here. (A substring match is OK, so &quot;MSFT&quot; </span></span><br><span class="line"><span class="comment"># matches &quot;MSFT&quot; and &quot;MSFT 5.0&quot;). This example sets the</span></span><br><span class="line"><span class="comment"># mtftp address to 0.0.0.0 for PXEClients.</span></span><br><span class="line"><span class="comment">#dhcp-option=vendor:PXEClient,1,0.0.0.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send microsoft-specific option to tell windows to release the DHCP lease</span></span><br><span class="line"><span class="comment"># when it shuts down. Note the &quot;i&quot; flag, to tell dnsmasq to send the</span></span><br><span class="line"><span class="comment"># value as a four-byte integer - that&#x27;s what microsoft wants. See</span></span><br><span class="line"><span class="comment"># http://technet2.microsoft.com/WindowsServer/en/library/a70f1bb7-d2d4-49f0-96d6-4b7414ecfaae1033.mspx?mfr=true</span></span><br><span class="line"><span class="comment">#dhcp-option=vendor:MSFT,2,1i</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send the Encapsulated-vendor-class ID needed by some configurations of</span></span><br><span class="line"><span class="comment"># Etherboot to allow is to recognise the DHCP server.</span></span><br><span class="line"><span class="comment">#dhcp-option=vendor:Etherboot,60,&quot;Etherboot&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Send options to PXELinux. Note that we need to send the options even</span></span><br><span class="line"><span class="comment"># though they don&#x27;t appear in the parameter request list, so we need</span></span><br><span class="line"><span class="comment"># to use dhcp-option-force here. </span></span><br><span class="line"><span class="comment"># See http://syslinux.zytor.com/pxe.php#special for details.</span></span><br><span class="line"><span class="comment"># Magic number - needed before anything else is recognised</span></span><br><span class="line"><span class="comment">#dhcp-option-force=208,f1:00:74:7e</span></span><br><span class="line"><span class="comment"># Configuration file name</span></span><br><span class="line"><span class="comment">#dhcp-option-force=209,configs/common</span></span><br><span class="line"><span class="comment"># Path prefix</span></span><br><span class="line"><span class="comment">#dhcp-option-force=210,/tftpboot/pxelinux/files/</span></span><br><span class="line"><span class="comment"># Reboot time. (Note &#x27;i&#x27; to send 32-bit value)</span></span><br><span class="line"><span class="comment">#dhcp-option-force=211,30i</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the boot filename for BOOTP. You will only need </span></span><br><span class="line"><span class="comment"># this is you want to boot machines over the network and you will need</span></span><br><span class="line"><span class="comment"># a TFTP server; either dnsmasq&#x27;s built in TFTP server or an</span></span><br><span class="line"><span class="comment"># external one. (See below for how to enable the TFTP server.)</span></span><br><span class="line"><span class="comment">#dhcp-boot=pxelinux.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Enable dnsmasq&#x27;s built-in TFTP server</span></span><br><span class="line"><span class="comment">#enable-tftp</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the root directory for files availble via FTP.</span></span><br><span class="line"><span class="comment">#tftp-root=/var/ftpd</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Make the TFTP server more secure: with this set, only files owned by</span></span><br><span class="line"><span class="comment"># the user dnsmasq is running as will be send over the net.</span></span><br><span class="line"><span class="comment">#tftp-secure</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the boot file name only when the &quot;red&quot; tag is set.</span></span><br><span class="line"><span class="comment">#dhcp-boot=net:red,pxelinux.red-net</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># An example of dhcp-boot with an external server: the name and IP</span></span><br><span class="line"><span class="comment"># address of the server are given after the filename.</span></span><br><span class="line"><span class="comment">#dhcp-boot=/var/ftpd/pxelinux.0,boothost,192.168.0.3</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the limit on DHCP leases, the default is 150</span></span><br><span class="line"><span class="comment">#dhcp-lease-max=150</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The DHCP server needs somewhere on disk to keep its lease database.</span></span><br><span class="line"><span class="comment"># This defaults to a sane location, but if you want to change it, use</span></span><br><span class="line"><span class="comment"># the line below.</span></span><br><span class="line"><span class="comment">#dhcp-leasefile=/var/lib/misc/dnsmasq.leases</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the DHCP server to authoritative mode. In this mode it will barge in</span></span><br><span class="line"><span class="comment"># and take over the lease for any client which broadcasts on the network,</span></span><br><span class="line"><span class="comment"># whether it has a record of the lease or not. This avoids long timeouts</span></span><br><span class="line"><span class="comment"># when a machine wakes up on a new network. DO NOT enable this if there&#x27;s</span></span><br><span class="line"><span class="comment"># the slighest chance that you might end up accidentally configuring a DHCP</span></span><br><span class="line"><span class="comment"># server for your campus/company accidentally. The ISC server uses</span></span><br><span class="line"><span class="comment"># the same option, and this URL provides more information:</span></span><br><span class="line"><span class="comment"># http://www.isc.org/index.pl?/sw/dhcp/authoritative.php</span></span><br><span class="line"><span class="comment">#dhcp-authoritative</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Run an executable when a DHCP lease is created or destroyed.</span></span><br><span class="line"><span class="comment"># The arguments sent to the script are &quot;add&quot; or &quot;del&quot;, </span></span><br><span class="line"><span class="comment"># then the MAC address, the IP address and finally the hostname</span></span><br><span class="line"><span class="comment"># if there is one. </span></span><br><span class="line"><span class="comment">#dhcp-script=/bin/echo</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the cachesize here.</span></span><br><span class="line"><span class="comment">#cache-size=150</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you want to disable negative caching, uncomment this.</span></span><br><span class="line"><span class="comment">#no-negcache</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Normally responses which come form /etc/hosts and the DHCP lease</span></span><br><span class="line"><span class="comment"># file have Time-To-Live set as zero, which conventionally means</span></span><br><span class="line"><span class="comment"># do not cache further. If you are happy to trade lower load on the</span></span><br><span class="line"><span class="comment"># server for potentially stale date, you can set a time-to-live (in</span></span><br><span class="line"><span class="comment"># seconds) here.</span></span><br><span class="line"><span class="comment">#local-ttl=</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you want dnsmasq to detect attempts by Verisign to send queries</span></span><br><span class="line"><span class="comment"># to unregistered .com and .net hosts to its sitefinder service and</span></span><br><span class="line"><span class="comment"># have dnsmasq instead return the correct NXDOMAIN response, uncomment</span></span><br><span class="line"><span class="comment"># this line. You can add similar lines to do the same for other</span></span><br><span class="line"><span class="comment"># registries which have implemented wildcard A records.</span></span><br><span class="line"><span class="comment">#bogus-nxdomain=64.94.110.11</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># If you want to fix up DNS results from upstream servers, use the</span></span><br><span class="line"><span class="comment"># alias option. This only works for IPv4.</span></span><br><span class="line"><span class="comment"># This alias makes a result of 1.2.3.4 appear as 5.6.7.8</span></span><br><span class="line"><span class="comment">#alias=1.2.3.4,5.6.7.8</span></span><br><span class="line"><span class="comment"># and this maps 1.2.3.x to 5.6.7.x</span></span><br><span class="line"><span class="comment">#alias=1.2.3.0,5.6.7.0,255.255.255.0</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># Change these lines if you want dnsmasq to serve MX records.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Return an MX record named &quot;maildomain.com&quot; with target</span></span><br><span class="line"><span class="comment"># servermachine.com and preference 50</span></span><br><span class="line"><span class="comment">#mx-host=maildomain.com,servermachine.com,50</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the default target for MX records created using the localmx option.</span></span><br><span class="line"><span class="comment">#mx-target=servermachine.com</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Return an MX record pointing to the mx-target for all local</span></span><br><span class="line"><span class="comment"># machines.</span></span><br><span class="line"><span class="comment">#localmx</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Return an MX record pointing to itself for all local machines.</span></span><br><span class="line"><span class="comment">#selfmx</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Change the following lines if you want dnsmasq to serve SRV</span></span><br><span class="line"><span class="comment"># records.  These are useful if you want to serve ldap requests for</span></span><br><span class="line"><span class="comment"># Active Directory and other windows-originated DNS requests.</span></span><br><span class="line"><span class="comment"># See RFC 2782.</span></span><br><span class="line"><span class="comment"># You may add multiple srv-host lines.</span></span><br><span class="line"><span class="comment"># The fields are &lt;name&gt;,&lt;target&gt;,&lt;port&gt;,&lt;priority&gt;,&lt;weight&gt;</span></span><br><span class="line"><span class="comment"># If the domain part if missing from the name (so that is just has the</span></span><br><span class="line"><span class="comment"># service and protocol sections) then the domain given by the domain=</span></span><br><span class="line"><span class="comment"># config option is used. (Note that expand-hosts does not need to be</span></span><br><span class="line"><span class="comment"># set for this to work.)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># A SRV record sending LDAP for the example.com domain to</span></span><br><span class="line"><span class="comment"># ldapserver.example.com port 289</span></span><br><span class="line"><span class="comment">#srv-host=_ldap._tcp.example.com,ldapserver.example.com,389</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># A SRV record sending LDAP for the example.com domain to</span></span><br><span class="line"><span class="comment"># ldapserver.example.com port 289 (using domain=)</span></span><br><span class="line"><span class="comment">#domain=example.com</span></span><br><span class="line"><span class="comment">#srv-host=_ldap._tcp,ldapserver.example.com,389</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Two SRV records for LDAP, each with different priorities</span></span><br><span class="line"><span class="comment">#srv-host=_ldap._tcp.example.com,ldapserver.example.com,389,1</span></span><br><span class="line"><span class="comment">#srv-host=_ldap._tcp.example.com,ldapserver.example.com,389,2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># A SRV record indicating that there is no LDAP server for the domain</span></span><br><span class="line"><span class="comment"># example.com</span></span><br><span class="line"><span class="comment">#srv-host=_ldap._tcp.example.com</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The following line shows how to make dnsmasq serve an arbitrary PTR</span></span><br><span class="line"><span class="comment"># record. This is useful for DNS-SD. (Note that the</span></span><br><span class="line"><span class="comment"># domain-name expansion done for SRV records _does_not</span></span><br><span class="line"><span class="comment"># occur for PTR records.)</span></span><br><span class="line"><span class="comment">#ptr-record=_http._tcp.dns-sd-services,&quot;New Employee Page._http._tcp.dns-sd-services&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Change the following lines to enable dnsmasq to serve TXT records.</span></span><br><span class="line"><span class="comment"># These are used for things like SPF and zeroconf. (Note that the</span></span><br><span class="line"><span class="comment"># domain-name expansion done for SRV records _does_not</span></span><br><span class="line"><span class="comment"># occur for TXT records.)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#Example SPF.</span></span><br><span class="line"><span class="comment">#txt-record=example.com,&quot;v=spf1 a -all&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#Example zeroconf</span></span><br><span class="line"><span class="comment">#txt-record=_http._tcp.example.com,name=value,paper=A4</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># For debugging purposes, log each DNS query as it passes through</span></span><br><span class="line"><span class="comment"># dnsmasq.</span></span><br><span class="line"><span class="comment">#log-queries</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Log lots of extra information about DHCP transactions.</span></span><br><span class="line"><span class="comment">#log-dhcp</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Include a another lot of configuration options.</span></span><br><span class="line"><span class="comment">#conf-file=/etc/dnsmasq.more.conf</span></span><br><span class="line"><span class="meta">conf-dir</span>=<span class="string">/etc/dnsmasq.d</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>如何在Docker中运行redis</title>
    <url>/2019/07/22/%E5%A6%82%E4%BD%95%E5%9C%A8Docker%E4%B8%AD%E8%BF%90%E8%A1%8Credis/</url>
    <content><![CDATA[<h3 id="获取redis镜像"><a href="#获取redis镜像" class="headerlink" title="获取redis镜像"></a>获取redis镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker pull redis:5.0.4</span><br></pre></td></tr></table></figure>
<h3 id="创建容器"><a href="#创建容器" class="headerlink" title="创建容器"></a>创建容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --restart&#x3D;always --name redis \</span><br><span class="line">-d -p 10.12.64.70:6379:6379 \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;redis&#x2F;0&#x2F;conf&#x2F;redis.conf:&#x2F;usr&#x2F;local&#x2F;etc&#x2F;redis&#x2F;redis.conf \</span><br><span class="line">-v &#x2F;data&#x2F;docker&#x2F;instance&#x2F;redis&#x2F;0&#x2F;data:&#x2F;data \</span><br><span class="line">redis:5.0.4</span><br></pre></td></tr></table></figure>
<blockquote>
<p>如果出现如下提示，参考备注(1)<br>WARNING: IPv4 forwarding is disabled. Networking will not work.</p>
</blockquote>
<h3 id="备注"><a href="#备注" class="headerlink" title="备注"></a>备注</h3><ol>
<li><p>打开**/usr/lib/sysctl.d/00-system.conf<strong>添加</strong>net.ipv4.ip_forward = 1**后重启网络服务</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Kernel sysctl configuration file</span><br><span class="line">#</span><br><span class="line"># For binary values, 0 is disabled, 1 is enabled.  See sysctl(8) and</span><br><span class="line"># sysctl.conf(5) for more details.</span><br><span class="line"></span><br><span class="line"># Disable netfilter on bridges.</span><br><span class="line">net.ipv4.ip_forward &#x3D; 1</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables &#x3D; 0</span><br><span class="line">net.bridge.bridge-nf-call-iptables &#x3D; 0</span><br><span class="line">net.bridge.bridge-nf-call-arptables &#x3D; 0</span><br></pre></td></tr></table></figure>
</li>
<li><p>获取配置内容方式</p>
</li>
</ol>
<ul>
<li><p>官方的配置文件<a href="http://download.redis.io/redis-stable/redis.conf" title="Redis configuration file example"><strong>redis.conf</strong></a></p>
</li>
<li><p>官方的配置文件内容</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Redis configuration file example.</span><br><span class="line">#</span><br><span class="line"># Note that in order to read the configuration file, Redis must be</span><br><span class="line"># started with the file path as first argument:</span><br><span class="line">#</span><br><span class="line"># .&#x2F;redis-server &#x2F;path&#x2F;to&#x2F;redis.conf</span><br><span class="line"></span><br><span class="line"># Note on units: when memory size is needed, it is possible to specify</span><br><span class="line"># it in the usual form of 1k 5GB 4M and so forth:</span><br><span class="line">#</span><br><span class="line"># 1k &#x3D;&gt; 1000 bytes</span><br><span class="line"># 1kb &#x3D;&gt; 1024 bytes</span><br><span class="line"># 1m &#x3D;&gt; 1000000 bytes</span><br><span class="line"># 1mb &#x3D;&gt; 1024*1024 bytes</span><br><span class="line"># 1g &#x3D;&gt; 1000000000 bytes</span><br><span class="line"># 1gb &#x3D;&gt; 1024*1024*1024 bytes</span><br><span class="line">#</span><br><span class="line"># units are case insensitive so 1GB 1Gb 1gB are all the same.</span><br><span class="line"></span><br><span class="line">################################## INCLUDES ###################################</span><br><span class="line"></span><br><span class="line"># Include one or more other config files here.  This is useful if you</span><br><span class="line"># have a standard template that goes to all Redis servers but also need</span><br><span class="line"># to customize a few per-server settings.  Include files can include</span><br><span class="line"># other files, so use this wisely.</span><br><span class="line">#</span><br><span class="line"># Notice option &quot;include&quot; won&#39;t be rewritten by command &quot;CONFIG REWRITE&quot;</span><br><span class="line"># from admin or Redis Sentinel. Since Redis always uses the last processed</span><br><span class="line"># line as value of a configuration directive, you&#39;d better put includes</span><br><span class="line"># at the beginning of this file to avoid overwriting config change at runtime.</span><br><span class="line">#</span><br><span class="line"># If instead you are interested in using includes to override configuration</span><br><span class="line"># options, it is better to use include as the last line.</span><br><span class="line">#</span><br><span class="line"># include &#x2F;path&#x2F;to&#x2F;local.conf</span><br><span class="line"># include &#x2F;path&#x2F;to&#x2F;other.conf</span><br><span class="line"></span><br><span class="line">################################## MODULES #####################################</span><br><span class="line"></span><br><span class="line"># Load modules at startup. If the server is not able to load modules</span><br><span class="line"># it will abort. It is possible to use multiple loadmodule directives.</span><br><span class="line">#</span><br><span class="line"># loadmodule &#x2F;path&#x2F;to&#x2F;my_module.so</span><br><span class="line"># loadmodule &#x2F;path&#x2F;to&#x2F;other_module.so</span><br><span class="line"></span><br><span class="line">################################## NETWORK #####################################</span><br><span class="line"></span><br><span class="line"># By default, if no &quot;bind&quot; configuration directive is specified, Redis listens</span><br><span class="line"># for connections from all the network interfaces available on the server.</span><br><span class="line"># It is possible to listen to just one or multiple selected interfaces using</span><br><span class="line"># the &quot;bind&quot; configuration directive, followed by one or more IP addresses.</span><br><span class="line">#</span><br><span class="line"># Examples:</span><br><span class="line">#</span><br><span class="line"># bind 192.168.1.100 10.0.0.1</span><br><span class="line"># bind 127.0.0.1 ::1</span><br><span class="line">#</span><br><span class="line"># ~~~ WARNING ~~~ If the computer running Redis is directly exposed to the</span><br><span class="line"># internet, binding to all the interfaces is dangerous and will expose the</span><br><span class="line"># instance to everybody on the internet. So by default we uncomment the</span><br><span class="line"># following bind directive, that will force Redis to listen only into</span><br><span class="line"># the IPv4 loopback interface address (this means Redis will be able to</span><br><span class="line"># accept connections only from clients running into the same computer it</span><br><span class="line"># is running).</span><br><span class="line">#</span><br><span class="line"># IF YOU ARE SURE YOU WANT YOUR INSTANCE TO LISTEN TO ALL THE INTERFACES</span><br><span class="line"># JUST COMMENT THE FOLLOWING LINE.</span><br><span class="line"># ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~</span><br><span class="line">bind 127.0.0.1</span><br><span class="line"></span><br><span class="line"># Protected mode is a layer of security protection, in order to avoid that</span><br><span class="line"># Redis instances left open on the internet are accessed and exploited.</span><br><span class="line">#</span><br><span class="line"># When protected mode is on and if:</span><br><span class="line">#</span><br><span class="line"># 1) The server is not binding explicitly to a set of addresses using the</span><br><span class="line">#    &quot;bind&quot; directive.</span><br><span class="line"># 2) No password is configured.</span><br><span class="line">#</span><br><span class="line"># The server only accepts connections from clients connecting from the</span><br><span class="line"># IPv4 and IPv6 loopback addresses 127.0.0.1 and ::1, and from Unix domain</span><br><span class="line"># sockets.</span><br><span class="line">#</span><br><span class="line"># By default protected mode is enabled. You should disable it only if</span><br><span class="line"># you are sure you want clients from other hosts to connect to Redis</span><br><span class="line"># even if no authentication is configured, nor a specific set of interfaces</span><br><span class="line"># are explicitly listed using the &quot;bind&quot; directive.</span><br><span class="line">protected-mode yes</span><br><span class="line"></span><br><span class="line"># Accept connections on the specified port, default is 6379 (IANA #815344).</span><br><span class="line"># If port 0 is specified Redis will not listen on a TCP socket.</span><br><span class="line">port 6379</span><br><span class="line"></span><br><span class="line"># TCP listen() backlog.</span><br><span class="line">#</span><br><span class="line"># In high requests-per-second environments you need an high backlog in order</span><br><span class="line"># to avoid slow clients connections issues. Note that the Linux kernel</span><br><span class="line"># will silently truncate it to the value of &#x2F;proc&#x2F;sys&#x2F;net&#x2F;core&#x2F;somaxconn so</span><br><span class="line"># make sure to raise both the value of somaxconn and tcp_max_syn_backlog</span><br><span class="line"># in order to get the desired effect.</span><br><span class="line">tcp-backlog 511</span><br><span class="line"></span><br><span class="line"># Unix socket.</span><br><span class="line">#</span><br><span class="line"># Specify the path for the Unix socket that will be used to listen for</span><br><span class="line"># incoming connections. There is no default, so Redis will not listen</span><br><span class="line"># on a unix socket when not specified.</span><br><span class="line">#</span><br><span class="line"># unixsocket &#x2F;tmp&#x2F;redis.sock</span><br><span class="line"># unixsocketperm 700</span><br><span class="line"></span><br><span class="line"># Close the connection after a client is idle for N seconds (0 to disable)</span><br><span class="line">timeout 0</span><br><span class="line"></span><br><span class="line"># TCP keepalive.</span><br><span class="line">#</span><br><span class="line"># If non-zero, use SO_KEEPALIVE to send TCP ACKs to clients in absence</span><br><span class="line"># of communication. This is useful for two reasons:</span><br><span class="line">#</span><br><span class="line"># 1) Detect dead peers.</span><br><span class="line"># 2) Take the connection alive from the point of view of network</span><br><span class="line">#    equipment in the middle.</span><br><span class="line">#</span><br><span class="line"># On Linux, the specified value (in seconds) is the period used to send ACKs.</span><br><span class="line"># Note that to close the connection the double of the time is needed.</span><br><span class="line"># On other kernels the period depends on the kernel configuration.</span><br><span class="line">#</span><br><span class="line"># A reasonable value for this option is 300 seconds, which is the new</span><br><span class="line"># Redis default starting with Redis 3.2.1.</span><br><span class="line">tcp-keepalive 300</span><br><span class="line"></span><br><span class="line">################################# GENERAL #####################################</span><br><span class="line"></span><br><span class="line"># By default Redis does not run as a daemon. Use &#39;yes&#39; if you need it.</span><br><span class="line"># Note that Redis will write a pid file in &#x2F;var&#x2F;run&#x2F;redis.pid when daemonized.</span><br><span class="line">daemonize no</span><br><span class="line"></span><br><span class="line"># If you run Redis from upstart or systemd, Redis can interact with your</span><br><span class="line"># supervision tree. Options:</span><br><span class="line">#   supervised no      - no supervision interaction</span><br><span class="line">#   supervised upstart - signal upstart by putting Redis into SIGSTOP mode</span><br><span class="line">#   supervised systemd - signal systemd by writing READY&#x3D;1 to $NOTIFY_SOCKET</span><br><span class="line">#   supervised auto    - detect upstart or systemd method based on</span><br><span class="line">#                        UPSTART_JOB or NOTIFY_SOCKET environment variables</span><br><span class="line"># Note: these supervision methods only signal &quot;process is ready.&quot;</span><br><span class="line">#       They do not enable continuous liveness pings back to your supervisor.</span><br><span class="line">supervised no</span><br><span class="line"></span><br><span class="line"># If a pid file is specified, Redis writes it where specified at startup</span><br><span class="line"># and removes it at exit.</span><br><span class="line">#</span><br><span class="line"># When the server runs non daemonized, no pid file is created if none is</span><br><span class="line"># specified in the configuration. When the server is daemonized, the pid file</span><br><span class="line"># is used even if not specified, defaulting to &quot;&#x2F;var&#x2F;run&#x2F;redis.pid&quot;.</span><br><span class="line">#</span><br><span class="line"># Creating a pid file is best effort: if Redis is not able to create it</span><br><span class="line"># nothing bad happens, the server will start and run normally.</span><br><span class="line">pidfile &#x2F;var&#x2F;run&#x2F;redis_6379.pid</span><br><span class="line"></span><br><span class="line"># Specify the server verbosity level.</span><br><span class="line"># This can be one of:</span><br><span class="line"># debug (a lot of information, useful for development&#x2F;testing)</span><br><span class="line"># verbose (many rarely useful info, but not a mess like the debug level)</span><br><span class="line"># notice (moderately verbose, what you want in production probably)</span><br><span class="line"># warning (only very important &#x2F; critical messages are logged)</span><br><span class="line">loglevel notice</span><br><span class="line"></span><br><span class="line"># Specify the log file name. Also the empty string can be used to force</span><br><span class="line"># Redis to log on the standard output. Note that if you use standard</span><br><span class="line"># output for logging but daemonize, logs will be sent to &#x2F;dev&#x2F;null</span><br><span class="line">logfile &quot;&quot;</span><br><span class="line"></span><br><span class="line"># To enable logging to the system logger, just set &#39;syslog-enabled&#39; to yes,</span><br><span class="line"># and optionally update the other syslog parameters to suit your needs.</span><br><span class="line"># syslog-enabled no</span><br><span class="line"></span><br><span class="line"># Specify the syslog identity.</span><br><span class="line"># syslog-ident redis</span><br><span class="line"></span><br><span class="line"># Specify the syslog facility. Must be USER or between LOCAL0-LOCAL7.</span><br><span class="line"># syslog-facility local0</span><br><span class="line"></span><br><span class="line"># Set the number of databases. The default database is DB 0, you can select</span><br><span class="line"># a different one on a per-connection basis using SELECT &lt;dbid&gt; where</span><br><span class="line"># dbid is a number between 0 and &#39;databases&#39;-1</span><br><span class="line">databases 16</span><br><span class="line"></span><br><span class="line"># By default Redis shows an ASCII art logo only when started to log to the</span><br><span class="line"># standard output and if the standard output is a TTY. Basically this means</span><br><span class="line"># that normally a logo is displayed only in interactive sessions.</span><br><span class="line">#</span><br><span class="line"># However it is possible to force the pre-4.0 behavior and always show a</span><br><span class="line"># ASCII art logo in startup logs by setting the following option to yes.</span><br><span class="line">always-show-logo yes</span><br><span class="line"></span><br><span class="line">################################ SNAPSHOTTING  ################################</span><br><span class="line">#</span><br><span class="line"># Save the DB on disk:</span><br><span class="line">#</span><br><span class="line">#   save &lt;seconds&gt; &lt;changes&gt;</span><br><span class="line">#</span><br><span class="line">#   Will save the DB if both the given number of seconds and the given</span><br><span class="line">#   number of write operations against the DB occurred.</span><br><span class="line">#</span><br><span class="line">#   In the example below the behaviour will be to save:</span><br><span class="line">#   after 900 sec (15 min) if at least 1 key changed</span><br><span class="line">#   after 300 sec (5 min) if at least 10 keys changed</span><br><span class="line">#   after 60 sec if at least 10000 keys changed</span><br><span class="line">#</span><br><span class="line">#   Note: you can disable saving completely by commenting out all &quot;save&quot; lines.</span><br><span class="line">#</span><br><span class="line">#   It is also possible to remove all the previously configured save</span><br><span class="line">#   points by adding a save directive with a single empty string argument</span><br><span class="line">#   like in the following example:</span><br><span class="line">#</span><br><span class="line">#   save &quot;&quot;</span><br><span class="line"></span><br><span class="line">save 900 1</span><br><span class="line">save 300 10</span><br><span class="line">save 60 10000</span><br><span class="line"></span><br><span class="line"># By default Redis will stop accepting writes if RDB snapshots are enabled</span><br><span class="line"># (at least one save point) and the latest background save failed.</span><br><span class="line"># This will make the user aware (in a hard way) that data is not persisting</span><br><span class="line"># on disk properly, otherwise chances are that no one will notice and some</span><br><span class="line"># disaster will happen.</span><br><span class="line">#</span><br><span class="line"># If the background saving process will start working again Redis will</span><br><span class="line"># automatically allow writes again.</span><br><span class="line">#</span><br><span class="line"># However if you have setup your proper monitoring of the Redis server</span><br><span class="line"># and persistence, you may want to disable this feature so that Redis will</span><br><span class="line"># continue to work as usual even if there are problems with disk,</span><br><span class="line"># permissions, and so forth.</span><br><span class="line">stop-writes-on-bgsave-error yes</span><br><span class="line"></span><br><span class="line"># Compress string objects using LZF when dump .rdb databases?</span><br><span class="line"># For default that&#39;s set to &#39;yes&#39; as it&#39;s almost always a win.</span><br><span class="line"># If you want to save some CPU in the saving child set it to &#39;no&#39; but</span><br><span class="line"># the dataset will likely be bigger if you have compressible values or keys.</span><br><span class="line">rdbcompression yes</span><br><span class="line"></span><br><span class="line"># Since version 5 of RDB a CRC64 checksum is placed at the end of the file.</span><br><span class="line"># This makes the format more resistant to corruption but there is a performance</span><br><span class="line"># hit to pay (around 10%) when saving and loading RDB files, so you can disable it</span><br><span class="line"># for maximum performances.</span><br><span class="line">#</span><br><span class="line"># RDB files created with checksum disabled have a checksum of zero that will</span><br><span class="line"># tell the loading code to skip the check.</span><br><span class="line">rdbchecksum yes</span><br><span class="line"></span><br><span class="line"># The filename where to dump the DB</span><br><span class="line">dbfilename dump.rdb</span><br><span class="line"></span><br><span class="line"># The working directory.</span><br><span class="line">#</span><br><span class="line"># The DB will be written inside this directory, with the filename specified</span><br><span class="line"># above using the &#39;dbfilename&#39; configuration directive.</span><br><span class="line">#</span><br><span class="line"># The Append Only File will also be created inside this directory.</span><br><span class="line">#</span><br><span class="line"># Note that you must specify a directory here, not a file name.</span><br><span class="line">dir .&#x2F;</span><br><span class="line"></span><br><span class="line">################################# REPLICATION #################################</span><br><span class="line"></span><br><span class="line"># Master-Replica replication. Use replicaof to make a Redis instance a copy of</span><br><span class="line"># another Redis server. A few things to understand ASAP about Redis replication.</span><br><span class="line">#</span><br><span class="line">#   +------------------+      +---------------+</span><br><span class="line">#   |      Master      | ---&gt; |    Replica    |</span><br><span class="line">#   | (receive writes) |      |  (exact copy) |</span><br><span class="line">#   +------------------+      +---------------+</span><br><span class="line">#</span><br><span class="line"># 1) Redis replication is asynchronous, but you can configure a master to</span><br><span class="line">#    stop accepting writes if it appears to be not connected with at least</span><br><span class="line">#    a given number of replicas.</span><br><span class="line"># 2) Redis replicas are able to perform a partial resynchronization with the</span><br><span class="line">#    master if the replication link is lost for a relatively small amount of</span><br><span class="line">#    time. You may want to configure the replication backlog size (see the next</span><br><span class="line">#    sections of this file) with a sensible value depending on your needs.</span><br><span class="line"># 3) Replication is automatic and does not need user intervention. After a</span><br><span class="line">#    network partition replicas automatically try to reconnect to masters</span><br><span class="line">#    and resynchronize with them.</span><br><span class="line">#</span><br><span class="line"># replicaof &lt;masterip&gt; &lt;masterport&gt;</span><br><span class="line"></span><br><span class="line"># If the master is password protected (using the &quot;requirepass&quot; configuration</span><br><span class="line"># directive below) it is possible to tell the replica to authenticate before</span><br><span class="line"># starting the replication synchronization process, otherwise the master will</span><br><span class="line"># refuse the replica request.</span><br><span class="line">#</span><br><span class="line"># masterauth &lt;master-password&gt;</span><br><span class="line"></span><br><span class="line"># When a replica loses its connection with the master, or when the replication</span><br><span class="line"># is still in progress, the replica can act in two different ways:</span><br><span class="line">#</span><br><span class="line"># 1) if replica-serve-stale-data is set to &#39;yes&#39; (the default) the replica will</span><br><span class="line">#    still reply to client requests, possibly with out of date data, or the</span><br><span class="line">#    data set may just be empty if this is the first synchronization.</span><br><span class="line">#</span><br><span class="line"># 2) if replica-serve-stale-data is set to &#39;no&#39; the replica will reply with</span><br><span class="line">#    an error &quot;SYNC with master in progress&quot; to all the kind of commands</span><br><span class="line">#    but to INFO, replicaOF, AUTH, PING, SHUTDOWN, REPLCONF, ROLE, CONFIG,</span><br><span class="line">#    SUBSCRIBE, UNSUBSCRIBE, PSUBSCRIBE, PUNSUBSCRIBE, PUBLISH, PUBSUB,</span><br><span class="line">#    COMMAND, POST, HOST: and LATENCY.</span><br><span class="line">#</span><br><span class="line">replica-serve-stale-data yes</span><br><span class="line"></span><br><span class="line"># You can configure a replica instance to accept writes or not. Writing against</span><br><span class="line"># a replica instance may be useful to store some ephemeral data (because data</span><br><span class="line"># written on a replica will be easily deleted after resync with the master) but</span><br><span class="line"># may also cause problems if clients are writing to it because of a</span><br><span class="line"># misconfiguration.</span><br><span class="line">#</span><br><span class="line"># Since Redis 2.6 by default replicas are read-only.</span><br><span class="line">#</span><br><span class="line"># Note: read only replicas are not designed to be exposed to untrusted clients</span><br><span class="line"># on the internet. It&#39;s just a protection layer against misuse of the instance.</span><br><span class="line"># Still a read only replica exports by default all the administrative commands</span><br><span class="line"># such as CONFIG, DEBUG, and so forth. To a limited extent you can improve</span><br><span class="line"># security of read only replicas using &#39;rename-command&#39; to shadow all the</span><br><span class="line"># administrative &#x2F; dangerous commands.</span><br><span class="line">replica-read-only yes</span><br><span class="line"></span><br><span class="line"># Replication SYNC strategy: disk or socket.</span><br><span class="line">#</span><br><span class="line"># -------------------------------------------------------</span><br><span class="line"># WARNING: DISKLESS REPLICATION IS EXPERIMENTAL CURRENTLY</span><br><span class="line"># -------------------------------------------------------</span><br><span class="line">#</span><br><span class="line"># New replicas and reconnecting replicas that are not able to continue the replication</span><br><span class="line"># process just receiving differences, need to do what is called a &quot;full</span><br><span class="line"># synchronization&quot;. An RDB file is transmitted from the master to the replicas.</span><br><span class="line"># The transmission can happen in two different ways:</span><br><span class="line">#</span><br><span class="line"># 1) Disk-backed: The Redis master creates a new process that writes the RDB</span><br><span class="line">#                 file on disk. Later the file is transferred by the parent</span><br><span class="line">#                 process to the replicas incrementally.</span><br><span class="line"># 2) Diskless: The Redis master creates a new process that directly writes the</span><br><span class="line">#              RDB file to replica sockets, without touching the disk at all.</span><br><span class="line">#</span><br><span class="line"># With disk-backed replication, while the RDB file is generated, more replicas</span><br><span class="line"># can be queued and served with the RDB file as soon as the current child producing</span><br><span class="line"># the RDB file finishes its work. With diskless replication instead once</span><br><span class="line"># the transfer starts, new replicas arriving will be queued and a new transfer</span><br><span class="line"># will start when the current one terminates.</span><br><span class="line">#</span><br><span class="line"># When diskless replication is used, the master waits a configurable amount of</span><br><span class="line"># time (in seconds) before starting the transfer in the hope that multiple replicas</span><br><span class="line"># will arrive and the transfer can be parallelized.</span><br><span class="line">#</span><br><span class="line"># With slow disks and fast (large bandwidth) networks, diskless replication</span><br><span class="line"># works better.</span><br><span class="line">repl-diskless-sync no</span><br><span class="line"></span><br><span class="line"># When diskless replication is enabled, it is possible to configure the delay</span><br><span class="line"># the server waits in order to spawn the child that transfers the RDB via socket</span><br><span class="line"># to the replicas.</span><br><span class="line">#</span><br><span class="line"># This is important since once the transfer starts, it is not possible to serve</span><br><span class="line"># new replicas arriving, that will be queued for the next RDB transfer, so the server</span><br><span class="line"># waits a delay in order to let more replicas arrive.</span><br><span class="line">#</span><br><span class="line"># The delay is specified in seconds, and by default is 5 seconds. To disable</span><br><span class="line"># it entirely just set it to 0 seconds and the transfer will start ASAP.</span><br><span class="line">repl-diskless-sync-delay 5</span><br><span class="line"></span><br><span class="line"># Replicas send PINGs to server in a predefined interval. It&#39;s possible to change</span><br><span class="line"># this interval with the repl_ping_replica_period option. The default value is 10</span><br><span class="line"># seconds.</span><br><span class="line">#</span><br><span class="line"># repl-ping-replica-period 10</span><br><span class="line"></span><br><span class="line"># The following option sets the replication timeout for:</span><br><span class="line">#</span><br><span class="line"># 1) Bulk transfer I&#x2F;O during SYNC, from the point of view of replica.</span><br><span class="line"># 2) Master timeout from the point of view of replicas (data, pings).</span><br><span class="line"># 3) Replica timeout from the point of view of masters (REPLCONF ACK pings).</span><br><span class="line">#</span><br><span class="line"># It is important to make sure that this value is greater than the value</span><br><span class="line"># specified for repl-ping-replica-period otherwise a timeout will be detected</span><br><span class="line"># every time there is low traffic between the master and the replica.</span><br><span class="line">#</span><br><span class="line"># repl-timeout 60</span><br><span class="line"></span><br><span class="line"># Disable TCP_NODELAY on the replica socket after SYNC?</span><br><span class="line">#</span><br><span class="line"># If you select &quot;yes&quot; Redis will use a smaller number of TCP packets and</span><br><span class="line"># less bandwidth to send data to replicas. But this can add a delay for</span><br><span class="line"># the data to appear on the replica side, up to 40 milliseconds with</span><br><span class="line"># Linux kernels using a default configuration.</span><br><span class="line">#</span><br><span class="line"># If you select &quot;no&quot; the delay for data to appear on the replica side will</span><br><span class="line"># be reduced but more bandwidth will be used for replication.</span><br><span class="line">#</span><br><span class="line"># By default we optimize for low latency, but in very high traffic conditions</span><br><span class="line"># or when the master and replicas are many hops away, turning this to &quot;yes&quot; may</span><br><span class="line"># be a good idea.</span><br><span class="line">repl-disable-tcp-nodelay no</span><br><span class="line"></span><br><span class="line"># Set the replication backlog size. The backlog is a buffer that accumulates</span><br><span class="line"># replica data when replicas are disconnected for some time, so that when a replica</span><br><span class="line"># wants to reconnect again, often a full resync is not needed, but a partial</span><br><span class="line"># resync is enough, just passing the portion of data the replica missed while</span><br><span class="line"># disconnected.</span><br><span class="line">#</span><br><span class="line"># The bigger the replication backlog, the longer the time the replica can be</span><br><span class="line"># disconnected and later be able to perform a partial resynchronization.</span><br><span class="line">#</span><br><span class="line"># The backlog is only allocated once there is at least a replica connected.</span><br><span class="line">#</span><br><span class="line"># repl-backlog-size 1mb</span><br><span class="line"></span><br><span class="line"># After a master has no longer connected replicas for some time, the backlog</span><br><span class="line"># will be freed. The following option configures the amount of seconds that</span><br><span class="line"># need to elapse, starting from the time the last replica disconnected, for</span><br><span class="line"># the backlog buffer to be freed.</span><br><span class="line">#</span><br><span class="line"># Note that replicas never free the backlog for timeout, since they may be</span><br><span class="line"># promoted to masters later, and should be able to correctly &quot;partially</span><br><span class="line"># resynchronize&quot; with the replicas: hence they should always accumulate backlog.</span><br><span class="line">#</span><br><span class="line"># A value of 0 means to never release the backlog.</span><br><span class="line">#</span><br><span class="line"># repl-backlog-ttl 3600</span><br><span class="line"></span><br><span class="line"># The replica priority is an integer number published by Redis in the INFO output.</span><br><span class="line"># It is used by Redis Sentinel in order to select a replica to promote into a</span><br><span class="line"># master if the master is no longer working correctly.</span><br><span class="line">#</span><br><span class="line"># A replica with a low priority number is considered better for promotion, so</span><br><span class="line"># for instance if there are three replicas with priority 10, 100, 25 Sentinel will</span><br><span class="line"># pick the one with priority 10, that is the lowest.</span><br><span class="line">#</span><br><span class="line"># However a special priority of 0 marks the replica as not able to perform the</span><br><span class="line"># role of master, so a replica with priority of 0 will never be selected by</span><br><span class="line"># Redis Sentinel for promotion.</span><br><span class="line">#</span><br><span class="line"># By default the priority is 100.</span><br><span class="line">replica-priority 100</span><br><span class="line"></span><br><span class="line"># It is possible for a master to stop accepting writes if there are less than</span><br><span class="line"># N replicas connected, having a lag less or equal than M seconds.</span><br><span class="line">#</span><br><span class="line"># The N replicas need to be in &quot;online&quot; state.</span><br><span class="line">#</span><br><span class="line"># The lag in seconds, that must be &lt;&#x3D; the specified value, is calculated from</span><br><span class="line"># the last ping received from the replica, that is usually sent every second.</span><br><span class="line">#</span><br><span class="line"># This option does not GUARANTEE that N replicas will accept the write, but</span><br><span class="line"># will limit the window of exposure for lost writes in case not enough replicas</span><br><span class="line"># are available, to the specified number of seconds.</span><br><span class="line">#</span><br><span class="line"># For example to require at least 3 replicas with a lag &lt;&#x3D; 10 seconds use:</span><br><span class="line">#</span><br><span class="line"># min-replicas-to-write 3</span><br><span class="line"># min-replicas-max-lag 10</span><br><span class="line">#</span><br><span class="line"># Setting one or the other to 0 disables the feature.</span><br><span class="line">#</span><br><span class="line"># By default min-replicas-to-write is set to 0 (feature disabled) and</span><br><span class="line"># min-replicas-max-lag is set to 10.</span><br><span class="line"></span><br><span class="line"># A Redis master is able to list the address and port of the attached</span><br><span class="line"># replicas in different ways. For example the &quot;INFO replication&quot; section</span><br><span class="line"># offers this information, which is used, among other tools, by</span><br><span class="line"># Redis Sentinel in order to discover replica instances.</span><br><span class="line"># Another place where this info is available is in the output of the</span><br><span class="line"># &quot;ROLE&quot; command of a master.</span><br><span class="line">#</span><br><span class="line"># The listed IP and address normally reported by a replica is obtained</span><br><span class="line"># in the following way:</span><br><span class="line">#</span><br><span class="line">#   IP: The address is auto detected by checking the peer address</span><br><span class="line">#   of the socket used by the replica to connect with the master.</span><br><span class="line">#</span><br><span class="line">#   Port: The port is communicated by the replica during the replication</span><br><span class="line">#   handshake, and is normally the port that the replica is using to</span><br><span class="line">#   listen for connections.</span><br><span class="line">#</span><br><span class="line"># However when port forwarding or Network Address Translation (NAT) is</span><br><span class="line"># used, the replica may be actually reachable via different IP and port</span><br><span class="line"># pairs. The following two options can be used by a replica in order to</span><br><span class="line"># report to its master a specific set of IP and port, so that both INFO</span><br><span class="line"># and ROLE will report those values.</span><br><span class="line">#</span><br><span class="line"># There is no need to use both the options if you need to override just</span><br><span class="line"># the port or the IP address.</span><br><span class="line">#</span><br><span class="line"># replica-announce-ip 5.5.5.5</span><br><span class="line"># replica-announce-port 1234</span><br><span class="line"></span><br><span class="line">################################## SECURITY ###################################</span><br><span class="line"></span><br><span class="line"># Require clients to issue AUTH &lt;PASSWORD&gt; before processing any other</span><br><span class="line"># commands.  This might be useful in environments in which you do not trust</span><br><span class="line"># others with access to the host running redis-server.</span><br><span class="line">#</span><br><span class="line"># This should stay commented out for backward compatibility and because most</span><br><span class="line"># people do not need auth (e.g. they run their own servers).</span><br><span class="line">#</span><br><span class="line"># Warning: since Redis is pretty fast an outside user can try up to</span><br><span class="line"># 150k passwords per second against a good box. This means that you should</span><br><span class="line"># use a very strong password otherwise it will be very easy to break.</span><br><span class="line">#</span><br><span class="line"># requirepass foobared</span><br><span class="line"></span><br><span class="line"># Command renaming.</span><br><span class="line">#</span><br><span class="line"># It is possible to change the name of dangerous commands in a shared</span><br><span class="line"># environment. For instance the CONFIG command may be renamed into something</span><br><span class="line"># hard to guess so that it will still be available for internal-use tools</span><br><span class="line"># but not available for general clients.</span><br><span class="line">#</span><br><span class="line"># Example:</span><br><span class="line">#</span><br><span class="line"># rename-command CONFIG b840fc02d524045429941cc15f59e41cb7be6c52</span><br><span class="line">#</span><br><span class="line"># It is also possible to completely kill a command by renaming it into</span><br><span class="line"># an empty string:</span><br><span class="line">#</span><br><span class="line"># rename-command CONFIG &quot;&quot;</span><br><span class="line">#</span><br><span class="line"># Please note that changing the name of commands that are logged into the</span><br><span class="line"># AOF file or transmitted to replicas may cause problems.</span><br><span class="line"></span><br><span class="line">################################### CLIENTS ####################################</span><br><span class="line"></span><br><span class="line"># Set the max number of connected clients at the same time. By default</span><br><span class="line"># this limit is set to 10000 clients, however if the Redis server is not</span><br><span class="line"># able to configure the process file limit to allow for the specified limit</span><br><span class="line"># the max number of allowed clients is set to the current file limit</span><br><span class="line"># minus 32 (as Redis reserves a few file descriptors for internal uses).</span><br><span class="line">#</span><br><span class="line"># Once the limit is reached Redis will close all the new connections sending</span><br><span class="line"># an error &#39;max number of clients reached&#39;.</span><br><span class="line">#</span><br><span class="line"># maxclients 10000</span><br><span class="line"></span><br><span class="line">############################## MEMORY MANAGEMENT ################################</span><br><span class="line"></span><br><span class="line"># Set a memory usage limit to the specified amount of bytes.</span><br><span class="line"># When the memory limit is reached Redis will try to remove keys</span><br><span class="line"># according to the eviction policy selected (see maxmemory-policy).</span><br><span class="line">#</span><br><span class="line"># If Redis can&#39;t remove keys according to the policy, or if the policy is</span><br><span class="line"># set to &#39;noeviction&#39;, Redis will start to reply with errors to commands</span><br><span class="line"># that would use more memory, like SET, LPUSH, and so on, and will continue</span><br><span class="line"># to reply to read-only commands like GET.</span><br><span class="line">#</span><br><span class="line"># This option is usually useful when using Redis as an LRU or LFU cache, or to</span><br><span class="line"># set a hard memory limit for an instance (using the &#39;noeviction&#39; policy).</span><br><span class="line">#</span><br><span class="line"># WARNING: If you have replicas attached to an instance with maxmemory on,</span><br><span class="line"># the size of the output buffers needed to feed the replicas are subtracted</span><br><span class="line"># from the used memory count, so that network problems &#x2F; resyncs will</span><br><span class="line"># not trigger a loop where keys are evicted, and in turn the output</span><br><span class="line"># buffer of replicas is full with DELs of keys evicted triggering the deletion</span><br><span class="line"># of more keys, and so forth until the database is completely emptied.</span><br><span class="line">#</span><br><span class="line"># In short... if you have replicas attached it is suggested that you set a lower</span><br><span class="line"># limit for maxmemory so that there is some free RAM on the system for replica</span><br><span class="line"># output buffers (but this is not needed if the policy is &#39;noeviction&#39;).</span><br><span class="line">#</span><br><span class="line"># maxmemory &lt;bytes&gt;</span><br><span class="line"></span><br><span class="line"># MAXMEMORY POLICY: how Redis will select what to remove when maxmemory</span><br><span class="line"># is reached. You can select among five behaviors:</span><br><span class="line">#</span><br><span class="line"># volatile-lru -&gt; Evict using approximated LRU among the keys with an expire set.</span><br><span class="line"># allkeys-lru -&gt; Evict any key using approximated LRU.</span><br><span class="line"># volatile-lfu -&gt; Evict using approximated LFU among the keys with an expire set.</span><br><span class="line"># allkeys-lfu -&gt; Evict any key using approximated LFU.</span><br><span class="line"># volatile-random -&gt; Remove a random key among the ones with an expire set.</span><br><span class="line"># allkeys-random -&gt; Remove a random key, any key.</span><br><span class="line"># volatile-ttl -&gt; Remove the key with the nearest expire time (minor TTL)</span><br><span class="line"># noeviction -&gt; Don&#39;t evict anything, just return an error on write operations.</span><br><span class="line">#</span><br><span class="line"># LRU means Least Recently Used</span><br><span class="line"># LFU means Least Frequently Used</span><br><span class="line">#</span><br><span class="line"># Both LRU, LFU and volatile-ttl are implemented using approximated</span><br><span class="line"># randomized algorithms.</span><br><span class="line">#</span><br><span class="line"># Note: with any of the above policies, Redis will return an error on write</span><br><span class="line">#       operations, when there are no suitable keys for eviction.</span><br><span class="line">#</span><br><span class="line">#       At the date of writing these commands are: set setnx setex append</span><br><span class="line">#       incr decr rpush lpush rpushx lpushx linsert lset rpoplpush sadd</span><br><span class="line">#       sinter sinterstore sunion sunionstore sdiff sdiffstore zadd zincrby</span><br><span class="line">#       zunionstore zinterstore hset hsetnx hmset hincrby incrby decrby</span><br><span class="line">#       getset mset msetnx exec sort</span><br><span class="line">#</span><br><span class="line"># The default is:</span><br><span class="line">#</span><br><span class="line"># maxmemory-policy noeviction</span><br><span class="line"></span><br><span class="line"># LRU, LFU and minimal TTL algorithms are not precise algorithms but approximated</span><br><span class="line"># algorithms (in order to save memory), so you can tune it for speed or</span><br><span class="line"># accuracy. For default Redis will check five keys and pick the one that was</span><br><span class="line"># used less recently, you can change the sample size using the following</span><br><span class="line"># configuration directive.</span><br><span class="line">#</span><br><span class="line"># The default of 5 produces good enough results. 10 Approximates very closely</span><br><span class="line"># true LRU but costs more CPU. 3 is faster but not very accurate.</span><br><span class="line">#</span><br><span class="line"># maxmemory-samples 5</span><br><span class="line"></span><br><span class="line"># Starting from Redis 5, by default a replica will ignore its maxmemory setting</span><br><span class="line"># (unless it is promoted to master after a failover or manually). It means</span><br><span class="line"># that the eviction of keys will be just handled by the master, sending the</span><br><span class="line"># DEL commands to the replica as keys evict in the master side.</span><br><span class="line">#</span><br><span class="line"># This behavior ensures that masters and replicas stay consistent, and is usually</span><br><span class="line"># what you want, however if your replica is writable, or you want the replica to have</span><br><span class="line"># a different memory setting, and you are sure all the writes performed to the</span><br><span class="line"># replica are idempotent, then you may change this default (but be sure to understand</span><br><span class="line"># what you are doing).</span><br><span class="line">#</span><br><span class="line"># Note that since the replica by default does not evict, it may end using more</span><br><span class="line"># memory than the one set via maxmemory (there are certain buffers that may</span><br><span class="line"># be larger on the replica, or data structures may sometimes take more memory and so</span><br><span class="line"># forth). So make sure you monitor your replicas and make sure they have enough</span><br><span class="line"># memory to never hit a real out-of-memory condition before the master hits</span><br><span class="line"># the configured maxmemory setting.</span><br><span class="line">#</span><br><span class="line"># replica-ignore-maxmemory yes</span><br><span class="line"></span><br><span class="line">############################# LAZY FREEING ####################################</span><br><span class="line"></span><br><span class="line"># Redis has two primitives to delete keys. One is called DEL and is a blocking</span><br><span class="line"># deletion of the object. It means that the server stops processing new commands</span><br><span class="line"># in order to reclaim all the memory associated with an object in a synchronous</span><br><span class="line"># way. If the key deleted is associated with a small object, the time needed</span><br><span class="line"># in order to execute the DEL command is very small and comparable to most other</span><br><span class="line"># O(1) or O(log_N) commands in Redis. However if the key is associated with an</span><br><span class="line"># aggregated value containing millions of elements, the server can block for</span><br><span class="line"># a long time (even seconds) in order to complete the operation.</span><br><span class="line">#</span><br><span class="line"># For the above reasons Redis also offers non blocking deletion primitives</span><br><span class="line"># such as UNLINK (non blocking DEL) and the ASYNC option of FLUSHALL and</span><br><span class="line"># FLUSHDB commands, in order to reclaim memory in background. Those commands</span><br><span class="line"># are executed in constant time. Another thread will incrementally free the</span><br><span class="line"># object in the background as fast as possible.</span><br><span class="line">#</span><br><span class="line"># DEL, UNLINK and ASYNC option of FLUSHALL and FLUSHDB are user-controlled.</span><br><span class="line"># It&#39;s up to the design of the application to understand when it is a good</span><br><span class="line"># idea to use one or the other. However the Redis server sometimes has to</span><br><span class="line"># delete keys or flush the whole database as a side effect of other operations.</span><br><span class="line"># Specifically Redis deletes objects independently of a user call in the</span><br><span class="line"># following scenarios:</span><br><span class="line">#</span><br><span class="line"># 1) On eviction, because of the maxmemory and maxmemory policy configurations,</span><br><span class="line">#    in order to make room for new data, without going over the specified</span><br><span class="line">#    memory limit.</span><br><span class="line"># 2) Because of expire: when a key with an associated time to live (see the</span><br><span class="line">#    EXPIRE command) must be deleted from memory.</span><br><span class="line"># 3) Because of a side effect of a command that stores data on a key that may</span><br><span class="line">#    already exist. For example the RENAME command may delete the old key</span><br><span class="line">#    content when it is replaced with another one. Similarly SUNIONSTORE</span><br><span class="line">#    or SORT with STORE option may delete existing keys. The SET command</span><br><span class="line">#    itself removes any old content of the specified key in order to replace</span><br><span class="line">#    it with the specified string.</span><br><span class="line"># 4) During replication, when a replica performs a full resynchronization with</span><br><span class="line">#    its master, the content of the whole database is removed in order to</span><br><span class="line">#    load the RDB file just transferred.</span><br><span class="line">#</span><br><span class="line"># In all the above cases the default is to delete objects in a blocking way,</span><br><span class="line"># like if DEL was called. However you can configure each case specifically</span><br><span class="line"># in order to instead release memory in a non-blocking way like if UNLINK</span><br><span class="line"># was called, using the following configuration directives:</span><br><span class="line"></span><br><span class="line">lazyfree-lazy-eviction no</span><br><span class="line">lazyfree-lazy-expire no</span><br><span class="line">lazyfree-lazy-server-del no</span><br><span class="line">replica-lazy-flush no</span><br><span class="line"></span><br><span class="line">############################## APPEND ONLY MODE ###############################</span><br><span class="line"></span><br><span class="line"># By default Redis asynchronously dumps the dataset on disk. This mode is</span><br><span class="line"># good enough in many applications, but an issue with the Redis process or</span><br><span class="line"># a power outage may result into a few minutes of writes lost (depending on</span><br><span class="line"># the configured save points).</span><br><span class="line">#</span><br><span class="line"># The Append Only File is an alternative persistence mode that provides</span><br><span class="line"># much better durability. For instance using the default data fsync policy</span><br><span class="line"># (see later in the config file) Redis can lose just one second of writes in a</span><br><span class="line"># dramatic event like a server power outage, or a single write if something</span><br><span class="line"># wrong with the Redis process itself happens, but the operating system is</span><br><span class="line"># still running correctly.</span><br><span class="line">#</span><br><span class="line"># AOF and RDB persistence can be enabled at the same time without problems.</span><br><span class="line"># If the AOF is enabled on startup Redis will load the AOF, that is the file</span><br><span class="line"># with the better durability guarantees.</span><br><span class="line">#</span><br><span class="line"># Please check http:&#x2F;&#x2F;redis.io&#x2F;topics&#x2F;persistence for more information.</span><br><span class="line"></span><br><span class="line">appendonly no</span><br><span class="line"></span><br><span class="line"># The name of the append only file (default: &quot;appendonly.aof&quot;)</span><br><span class="line"></span><br><span class="line">appendfilename &quot;appendonly.aof&quot;</span><br><span class="line"></span><br><span class="line"># The fsync() call tells the Operating System to actually write data on disk</span><br><span class="line"># instead of waiting for more data in the output buffer. Some OS will really flush</span><br><span class="line"># data on disk, some other OS will just try to do it ASAP.</span><br><span class="line">#</span><br><span class="line"># Redis supports three different modes:</span><br><span class="line">#</span><br><span class="line"># no: don&#39;t fsync, just let the OS flush the data when it wants. Faster.</span><br><span class="line"># always: fsync after every write to the append only log. Slow, Safest.</span><br><span class="line"># everysec: fsync only one time every second. Compromise.</span><br><span class="line">#</span><br><span class="line"># The default is &quot;everysec&quot;, as that&#39;s usually the right compromise between</span><br><span class="line"># speed and data safety. It&#39;s up to you to understand if you can relax this to</span><br><span class="line"># &quot;no&quot; that will let the operating system flush the output buffer when</span><br><span class="line"># it wants, for better performances (but if you can live with the idea of</span><br><span class="line"># some data loss consider the default persistence mode that&#39;s snapshotting),</span><br><span class="line"># or on the contrary, use &quot;always&quot; that&#39;s very slow but a bit safer than</span><br><span class="line"># everysec.</span><br><span class="line">#</span><br><span class="line"># More details please check the following article:</span><br><span class="line"># http:&#x2F;&#x2F;antirez.com&#x2F;post&#x2F;redis-persistence-demystified.html</span><br><span class="line">#</span><br><span class="line"># If unsure, use &quot;everysec&quot;.</span><br><span class="line"></span><br><span class="line"># appendfsync always</span><br><span class="line">appendfsync everysec</span><br><span class="line"># appendfsync no</span><br><span class="line"></span><br><span class="line"># When the AOF fsync policy is set to always or everysec, and a background</span><br><span class="line"># saving process (a background save or AOF log background rewriting) is</span><br><span class="line"># performing a lot of I&#x2F;O against the disk, in some Linux configurations</span><br><span class="line"># Redis may block too long on the fsync() call. Note that there is no fix for</span><br><span class="line"># this currently, as even performing fsync in a different thread will block</span><br><span class="line"># our synchronous write(2) call.</span><br><span class="line">#</span><br><span class="line"># In order to mitigate this problem it&#39;s possible to use the following option</span><br><span class="line"># that will prevent fsync() from being called in the main process while a</span><br><span class="line"># BGSAVE or BGREWRITEAOF is in progress.</span><br><span class="line">#</span><br><span class="line"># This means that while another child is saving, the durability of Redis is</span><br><span class="line"># the same as &quot;appendfsync none&quot;. In practical terms, this means that it is</span><br><span class="line"># possible to lose up to 30 seconds of log in the worst scenario (with the</span><br><span class="line"># default Linux settings).</span><br><span class="line">#</span><br><span class="line"># If you have latency problems turn this to &quot;yes&quot;. Otherwise leave it as</span><br><span class="line"># &quot;no&quot; that is the safest pick from the point of view of durability.</span><br><span class="line"></span><br><span class="line">no-appendfsync-on-rewrite no</span><br><span class="line"></span><br><span class="line"># Automatic rewrite of the append only file.</span><br><span class="line"># Redis is able to automatically rewrite the log file implicitly calling</span><br><span class="line"># BGREWRITEAOF when the AOF log size grows by the specified percentage.</span><br><span class="line">#</span><br><span class="line"># This is how it works: Redis remembers the size of the AOF file after the</span><br><span class="line"># latest rewrite (if no rewrite has happened since the restart, the size of</span><br><span class="line"># the AOF at startup is used).</span><br><span class="line">#</span><br><span class="line"># This base size is compared to the current size. If the current size is</span><br><span class="line"># bigger than the specified percentage, the rewrite is triggered. Also</span><br><span class="line"># you need to specify a minimal size for the AOF file to be rewritten, this</span><br><span class="line"># is useful to avoid rewriting the AOF file even if the percentage increase</span><br><span class="line"># is reached but it is still pretty small.</span><br><span class="line">#</span><br><span class="line"># Specify a percentage of zero in order to disable the automatic AOF</span><br><span class="line"># rewrite feature.</span><br><span class="line"></span><br><span class="line">auto-aof-rewrite-percentage 100</span><br><span class="line">auto-aof-rewrite-min-size 64mb</span><br><span class="line"></span><br><span class="line"># An AOF file may be found to be truncated at the end during the Redis</span><br><span class="line"># startup process, when the AOF data gets loaded back into memory.</span><br><span class="line"># This may happen when the system where Redis is running</span><br><span class="line"># crashes, especially when an ext4 filesystem is mounted without the</span><br><span class="line"># data&#x3D;ordered option (however this can&#39;t happen when Redis itself</span><br><span class="line"># crashes or aborts but the operating system still works correctly).</span><br><span class="line">#</span><br><span class="line"># Redis can either exit with an error when this happens, or load as much</span><br><span class="line"># data as possible (the default now) and start if the AOF file is found</span><br><span class="line"># to be truncated at the end. The following option controls this behavior.</span><br><span class="line">#</span><br><span class="line"># If aof-load-truncated is set to yes, a truncated AOF file is loaded and</span><br><span class="line"># the Redis server starts emitting a log to inform the user of the event.</span><br><span class="line"># Otherwise if the option is set to no, the server aborts with an error</span><br><span class="line"># and refuses to start. When the option is set to no, the user requires</span><br><span class="line"># to fix the AOF file using the &quot;redis-check-aof&quot; utility before to restart</span><br><span class="line"># the server.</span><br><span class="line">#</span><br><span class="line"># Note that if the AOF file will be found to be corrupted in the middle</span><br><span class="line"># the server will still exit with an error. This option only applies when</span><br><span class="line"># Redis will try to read more data from the AOF file but not enough bytes</span><br><span class="line"># will be found.</span><br><span class="line">aof-load-truncated yes</span><br><span class="line"></span><br><span class="line"># When rewriting the AOF file, Redis is able to use an RDB preamble in the</span><br><span class="line"># AOF file for faster rewrites and recoveries. When this option is turned</span><br><span class="line"># on the rewritten AOF file is composed of two different stanzas:</span><br><span class="line">#</span><br><span class="line">#   [RDB file][AOF tail]</span><br><span class="line">#</span><br><span class="line"># When loading Redis recognizes that the AOF file starts with the &quot;REDIS&quot;</span><br><span class="line"># string and loads the prefixed RDB file, and continues loading the AOF</span><br><span class="line"># tail.</span><br><span class="line">aof-use-rdb-preamble yes</span><br><span class="line"></span><br><span class="line">################################ LUA SCRIPTING  ###############################</span><br><span class="line"></span><br><span class="line"># Max execution time of a Lua script in milliseconds.</span><br><span class="line">#</span><br><span class="line"># If the maximum execution time is reached Redis will log that a script is</span><br><span class="line"># still in execution after the maximum allowed time and will start to</span><br><span class="line"># reply to queries with an error.</span><br><span class="line">#</span><br><span class="line"># When a long running script exceeds the maximum execution time only the</span><br><span class="line"># SCRIPT KILL and SHUTDOWN NOSAVE commands are available. The first can be</span><br><span class="line"># used to stop a script that did not yet called write commands. The second</span><br><span class="line"># is the only way to shut down the server in the case a write command was</span><br><span class="line"># already issued by the script but the user doesn&#39;t want to wait for the natural</span><br><span class="line"># termination of the script.</span><br><span class="line">#</span><br><span class="line"># Set it to 0 or a negative value for unlimited execution without warnings.</span><br><span class="line">lua-time-limit 5000</span><br><span class="line"></span><br><span class="line">################################ REDIS CLUSTER  ###############################</span><br><span class="line"></span><br><span class="line"># Normal Redis instances can&#39;t be part of a Redis Cluster; only nodes that are</span><br><span class="line"># started as cluster nodes can. In order to start a Redis instance as a</span><br><span class="line"># cluster node enable the cluster support uncommenting the following:</span><br><span class="line">#</span><br><span class="line"># cluster-enabled yes</span><br><span class="line"></span><br><span class="line"># Every cluster node has a cluster configuration file. This file is not</span><br><span class="line"># intended to be edited by hand. It is created and updated by Redis nodes.</span><br><span class="line"># Every Redis Cluster node requires a different cluster configuration file.</span><br><span class="line"># Make sure that instances running in the same system do not have</span><br><span class="line"># overlapping cluster configuration file names.</span><br><span class="line">#</span><br><span class="line"># cluster-config-file nodes-6379.conf</span><br><span class="line"></span><br><span class="line"># Cluster node timeout is the amount of milliseconds a node must be unreachable</span><br><span class="line"># for it to be considered in failure state.</span><br><span class="line"># Most other internal time limits are multiple of the node timeout.</span><br><span class="line">#</span><br><span class="line"># cluster-node-timeout 15000</span><br><span class="line"></span><br><span class="line"># A replica of a failing master will avoid to start a failover if its data</span><br><span class="line"># looks too old.</span><br><span class="line">#</span><br><span class="line"># There is no simple way for a replica to actually have an exact measure of</span><br><span class="line"># its &quot;data age&quot;, so the following two checks are performed:</span><br><span class="line">#</span><br><span class="line"># 1) If there are multiple replicas able to failover, they exchange messages</span><br><span class="line">#    in order to try to give an advantage to the replica with the best</span><br><span class="line">#    replication offset (more data from the master processed).</span><br><span class="line">#    Replicas will try to get their rank by offset, and apply to the start</span><br><span class="line">#    of the failover a delay proportional to their rank.</span><br><span class="line">#</span><br><span class="line"># 2) Every single replica computes the time of the last interaction with</span><br><span class="line">#    its master. This can be the last ping or command received (if the master</span><br><span class="line">#    is still in the &quot;connected&quot; state), or the time that elapsed since the</span><br><span class="line">#    disconnection with the master (if the replication link is currently down).</span><br><span class="line">#    If the last interaction is too old, the replica will not try to failover</span><br><span class="line">#    at all.</span><br><span class="line">#</span><br><span class="line"># The point &quot;2&quot; can be tuned by user. Specifically a replica will not perform</span><br><span class="line"># the failover if, since the last interaction with the master, the time</span><br><span class="line"># elapsed is greater than:</span><br><span class="line">#</span><br><span class="line">#   (node-timeout * replica-validity-factor) + repl-ping-replica-period</span><br><span class="line">#</span><br><span class="line"># So for example if node-timeout is 30 seconds, and the replica-validity-factor</span><br><span class="line"># is 10, and assuming a default repl-ping-replica-period of 10 seconds, the</span><br><span class="line"># replica will not try to failover if it was not able to talk with the master</span><br><span class="line"># for longer than 310 seconds.</span><br><span class="line">#</span><br><span class="line"># A large replica-validity-factor may allow replicas with too old data to failover</span><br><span class="line"># a master, while a too small value may prevent the cluster from being able to</span><br><span class="line"># elect a replica at all.</span><br><span class="line">#</span><br><span class="line"># For maximum availability, it is possible to set the replica-validity-factor</span><br><span class="line"># to a value of 0, which means, that replicas will always try to failover the</span><br><span class="line"># master regardless of the last time they interacted with the master.</span><br><span class="line"># (However they&#39;ll always try to apply a delay proportional to their</span><br><span class="line"># offset rank).</span><br><span class="line">#</span><br><span class="line"># Zero is the only value able to guarantee that when all the partitions heal</span><br><span class="line"># the cluster will always be able to continue.</span><br><span class="line">#</span><br><span class="line"># cluster-replica-validity-factor 10</span><br><span class="line"></span><br><span class="line"># Cluster replicas are able to migrate to orphaned masters, that are masters</span><br><span class="line"># that are left without working replicas. This improves the cluster ability</span><br><span class="line"># to resist to failures as otherwise an orphaned master can&#39;t be failed over</span><br><span class="line"># in case of failure if it has no working replicas.</span><br><span class="line">#</span><br><span class="line"># Replicas migrate to orphaned masters only if there are still at least a</span><br><span class="line"># given number of other working replicas for their old master. This number</span><br><span class="line"># is the &quot;migration barrier&quot;. A migration barrier of 1 means that a replica</span><br><span class="line"># will migrate only if there is at least 1 other working replica for its master</span><br><span class="line"># and so forth. It usually reflects the number of replicas you want for every</span><br><span class="line"># master in your cluster.</span><br><span class="line">#</span><br><span class="line"># Default is 1 (replicas migrate only if their masters remain with at least</span><br><span class="line"># one replica). To disable migration just set it to a very large value.</span><br><span class="line"># A value of 0 can be set but is useful only for debugging and dangerous</span><br><span class="line"># in production.</span><br><span class="line">#</span><br><span class="line"># cluster-migration-barrier 1</span><br><span class="line"></span><br><span class="line"># By default Redis Cluster nodes stop accepting queries if they detect there</span><br><span class="line"># is at least an hash slot uncovered (no available node is serving it).</span><br><span class="line"># This way if the cluster is partially down (for example a range of hash slots</span><br><span class="line"># are no longer covered) all the cluster becomes, eventually, unavailable.</span><br><span class="line"># It automatically returns available as soon as all the slots are covered again.</span><br><span class="line">#</span><br><span class="line"># However sometimes you want the subset of the cluster which is working,</span><br><span class="line"># to continue to accept queries for the part of the key space that is still</span><br><span class="line"># covered. In order to do so, just set the cluster-require-full-coverage</span><br><span class="line"># option to no.</span><br><span class="line">#</span><br><span class="line"># cluster-require-full-coverage yes</span><br><span class="line"></span><br><span class="line"># This option, when set to yes, prevents replicas from trying to failover its</span><br><span class="line"># master during master failures. However the master can still perform a</span><br><span class="line"># manual failover, if forced to do so.</span><br><span class="line">#</span><br><span class="line"># This is useful in different scenarios, especially in the case of multiple</span><br><span class="line"># data center operations, where we want one side to never be promoted if not</span><br><span class="line"># in the case of a total DC failure.</span><br><span class="line">#</span><br><span class="line"># cluster-replica-no-failover no</span><br><span class="line"></span><br><span class="line"># In order to setup your cluster make sure to read the documentation</span><br><span class="line"># available at http:&#x2F;&#x2F;redis.io web site.</span><br><span class="line"></span><br><span class="line">########################## CLUSTER DOCKER&#x2F;NAT support  ########################</span><br><span class="line"></span><br><span class="line"># In certain deployments, Redis Cluster nodes address discovery fails, because</span><br><span class="line"># addresses are NAT-ted or because ports are forwarded (the typical case is</span><br><span class="line"># Docker and other containers).</span><br><span class="line">#</span><br><span class="line"># In order to make Redis Cluster working in such environments, a static</span><br><span class="line"># configuration where each node knows its public address is needed. The</span><br><span class="line"># following two options are used for this scope, and are:</span><br><span class="line">#</span><br><span class="line"># * cluster-announce-ip</span><br><span class="line"># * cluster-announce-port</span><br><span class="line"># * cluster-announce-bus-port</span><br><span class="line">#</span><br><span class="line"># Each instruct the node about its address, client port, and cluster message</span><br><span class="line"># bus port. The information is then published in the header of the bus packets</span><br><span class="line"># so that other nodes will be able to correctly map the address of the node</span><br><span class="line"># publishing the information.</span><br><span class="line">#</span><br><span class="line"># If the above options are not used, the normal Redis Cluster auto-detection</span><br><span class="line"># will be used instead.</span><br><span class="line">#</span><br><span class="line"># Note that when remapped, the bus port may not be at the fixed offset of</span><br><span class="line"># clients port + 10000, so you can specify any port and bus-port depending</span><br><span class="line"># on how they get remapped. If the bus-port is not set, a fixed offset of</span><br><span class="line"># 10000 will be used as usually.</span><br><span class="line">#</span><br><span class="line"># Example:</span><br><span class="line">#</span><br><span class="line"># cluster-announce-ip 10.1.1.5</span><br><span class="line"># cluster-announce-port 6379</span><br><span class="line"># cluster-announce-bus-port 6380</span><br><span class="line"></span><br><span class="line">################################## SLOW LOG ###################################</span><br><span class="line"></span><br><span class="line"># The Redis Slow Log is a system to log queries that exceeded a specified</span><br><span class="line"># execution time. The execution time does not include the I&#x2F;O operations</span><br><span class="line"># like talking with the client, sending the reply and so forth,</span><br><span class="line"># but just the time needed to actually execute the command (this is the only</span><br><span class="line"># stage of command execution where the thread is blocked and can not serve</span><br><span class="line"># other requests in the meantime).</span><br><span class="line">#</span><br><span class="line"># You can configure the slow log with two parameters: one tells Redis</span><br><span class="line"># what is the execution time, in microseconds, to exceed in order for the</span><br><span class="line"># command to get logged, and the other parameter is the length of the</span><br><span class="line"># slow log. When a new command is logged the oldest one is removed from the</span><br><span class="line"># queue of logged commands.</span><br><span class="line"></span><br><span class="line"># The following time is expressed in microseconds, so 1000000 is equivalent</span><br><span class="line"># to one second. Note that a negative number disables the slow log, while</span><br><span class="line"># a value of zero forces the logging of every command.</span><br><span class="line">slowlog-log-slower-than 10000</span><br><span class="line"></span><br><span class="line"># There is no limit to this length. Just be aware that it will consume memory.</span><br><span class="line"># You can reclaim memory used by the slow log with SLOWLOG RESET.</span><br><span class="line">slowlog-max-len 128</span><br><span class="line"></span><br><span class="line">################################ LATENCY MONITOR ##############################</span><br><span class="line"></span><br><span class="line"># The Redis latency monitoring subsystem samples different operations</span><br><span class="line"># at runtime in order to collect data related to possible sources of</span><br><span class="line"># latency of a Redis instance.</span><br><span class="line">#</span><br><span class="line"># Via the LATENCY command this information is available to the user that can</span><br><span class="line"># print graphs and obtain reports.</span><br><span class="line">#</span><br><span class="line"># The system only logs operations that were performed in a time equal or</span><br><span class="line"># greater than the amount of milliseconds specified via the</span><br><span class="line"># latency-monitor-threshold configuration directive. When its value is set</span><br><span class="line"># to zero, the latency monitor is turned off.</span><br><span class="line">#</span><br><span class="line"># By default latency monitoring is disabled since it is mostly not needed</span><br><span class="line"># if you don&#39;t have latency issues, and collecting data has a performance</span><br><span class="line"># impact, that while very small, can be measured under big load. Latency</span><br><span class="line"># monitoring can easily be enabled at runtime using the command</span><br><span class="line"># &quot;CONFIG SET latency-monitor-threshold &lt;milliseconds&gt;&quot; if needed.</span><br><span class="line">latency-monitor-threshold 0</span><br><span class="line"></span><br><span class="line">############################# EVENT NOTIFICATION ##############################</span><br><span class="line"></span><br><span class="line"># Redis can notify Pub&#x2F;Sub clients about events happening in the key space.</span><br><span class="line"># This feature is documented at http:&#x2F;&#x2F;redis.io&#x2F;topics&#x2F;notifications</span><br><span class="line">#</span><br><span class="line"># For instance if keyspace events notification is enabled, and a client</span><br><span class="line"># performs a DEL operation on key &quot;foo&quot; stored in the Database 0, two</span><br><span class="line"># messages will be published via Pub&#x2F;Sub:</span><br><span class="line">#</span><br><span class="line"># PUBLISH __keyspace@0__:foo del</span><br><span class="line"># PUBLISH __keyevent@0__:del foo</span><br><span class="line">#</span><br><span class="line"># It is possible to select the events that Redis will notify among a set</span><br><span class="line"># of classes. Every class is identified by a single character:</span><br><span class="line">#</span><br><span class="line">#  K     Keyspace events, published with __keyspace@&lt;db&gt;__ prefix.</span><br><span class="line">#  E     Keyevent events, published with __keyevent@&lt;db&gt;__ prefix.</span><br><span class="line">#  g     Generic commands (non-type specific) like DEL, EXPIRE, RENAME, ...</span><br><span class="line">#  $     String commands</span><br><span class="line">#  l     List commands</span><br><span class="line">#  s     Set commands</span><br><span class="line">#  h     Hash commands</span><br><span class="line">#  z     Sorted set commands</span><br><span class="line">#  x     Expired events (events generated every time a key expires)</span><br><span class="line">#  e     Evicted events (events generated when a key is evicted for maxmemory)</span><br><span class="line">#  A     Alias for g$lshzxe, so that the &quot;AKE&quot; string means all the events.</span><br><span class="line">#</span><br><span class="line">#  The &quot;notify-keyspace-events&quot; takes as argument a string that is composed</span><br><span class="line">#  of zero or multiple characters. The empty string means that notifications</span><br><span class="line">#  are disabled.</span><br><span class="line">#</span><br><span class="line">#  Example: to enable list and generic events, from the point of view of the</span><br><span class="line">#           event name, use:</span><br><span class="line">#</span><br><span class="line">#  notify-keyspace-events Elg</span><br><span class="line">#</span><br><span class="line">#  Example 2: to get the stream of the expired keys subscribing to channel</span><br><span class="line">#             name __keyevent@0__:expired use:</span><br><span class="line">#</span><br><span class="line">#  notify-keyspace-events Ex</span><br><span class="line">#</span><br><span class="line">#  By default all notifications are disabled because most users don&#39;t need</span><br><span class="line">#  this feature and the feature has some overhead. Note that if you don&#39;t</span><br><span class="line">#  specify at least one of K or E, no events will be delivered.</span><br><span class="line">notify-keyspace-events &quot;&quot;</span><br><span class="line"></span><br><span class="line">############################### ADVANCED CONFIG ###############################</span><br><span class="line"></span><br><span class="line"># Hashes are encoded using a memory efficient data structure when they have a</span><br><span class="line"># small number of entries, and the biggest entry does not exceed a given</span><br><span class="line"># threshold. These thresholds can be configured using the following directives.</span><br><span class="line">hash-max-ziplist-entries 512</span><br><span class="line">hash-max-ziplist-value 64</span><br><span class="line"></span><br><span class="line"># Lists are also encoded in a special way to save a lot of space.</span><br><span class="line"># The number of entries allowed per internal list node can be specified</span><br><span class="line"># as a fixed maximum size or a maximum number of elements.</span><br><span class="line"># For a fixed maximum size, use -5 through -1, meaning:</span><br><span class="line"># -5: max size: 64 Kb  &lt;-- not recommended for normal workloads</span><br><span class="line"># -4: max size: 32 Kb  &lt;-- not recommended</span><br><span class="line"># -3: max size: 16 Kb  &lt;-- probably not recommended</span><br><span class="line"># -2: max size: 8 Kb   &lt;-- good</span><br><span class="line"># -1: max size: 4 Kb   &lt;-- good</span><br><span class="line"># Positive numbers mean store up to _exactly_ that number of elements</span><br><span class="line"># per list node.</span><br><span class="line"># The highest performing option is usually -2 (8 Kb size) or -1 (4 Kb size),</span><br><span class="line"># but if your use case is unique, adjust the settings as necessary.</span><br><span class="line">list-max-ziplist-size -2</span><br><span class="line"></span><br><span class="line"># Lists may also be compressed.</span><br><span class="line"># Compress depth is the number of quicklist ziplist nodes from *each* side of</span><br><span class="line"># the list to *exclude* from compression.  The head and tail of the list</span><br><span class="line"># are always uncompressed for fast push&#x2F;pop operations.  Settings are:</span><br><span class="line"># 0: disable all list compression</span><br><span class="line"># 1: depth 1 means &quot;don&#39;t start compressing until after 1 node into the list,</span><br><span class="line">#    going from either the head or tail&quot;</span><br><span class="line">#    So: [head]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[tail]</span><br><span class="line">#    [head], [tail] will always be uncompressed; inner nodes will compress.</span><br><span class="line"># 2: [head]-&gt;[next]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[prev]-&gt;[tail]</span><br><span class="line">#    2 here means: don&#39;t compress head or head-&gt;next or tail-&gt;prev or tail,</span><br><span class="line">#    but compress all nodes between them.</span><br><span class="line"># 3: [head]-&gt;[next]-&gt;[next]-&gt;node-&gt;node-&gt;...-&gt;node-&gt;[prev]-&gt;[prev]-&gt;[tail]</span><br><span class="line"># etc.</span><br><span class="line">list-compress-depth 0</span><br><span class="line"></span><br><span class="line"># Sets have a special encoding in just one case: when a set is composed</span><br><span class="line"># of just strings that happen to be integers in radix 10 in the range</span><br><span class="line"># of 64 bit signed integers.</span><br><span class="line"># The following configuration setting sets the limit in the size of the</span><br><span class="line"># set in order to use this special memory saving encoding.</span><br><span class="line">set-max-intset-entries 512</span><br><span class="line"></span><br><span class="line"># Similarly to hashes and lists, sorted sets are also specially encoded in</span><br><span class="line"># order to save a lot of space. This encoding is only used when the length and</span><br><span class="line"># elements of a sorted set are below the following limits:</span><br><span class="line">zset-max-ziplist-entries 128</span><br><span class="line">zset-max-ziplist-value 64</span><br><span class="line"></span><br><span class="line"># HyperLogLog sparse representation bytes limit. The limit includes the</span><br><span class="line"># 16 bytes header. When an HyperLogLog using the sparse representation crosses</span><br><span class="line"># this limit, it is converted into the dense representation.</span><br><span class="line">#</span><br><span class="line"># A value greater than 16000 is totally useless, since at that point the</span><br><span class="line"># dense representation is more memory efficient.</span><br><span class="line">#</span><br><span class="line"># The suggested value is ~ 3000 in order to have the benefits of</span><br><span class="line"># the space efficient encoding without slowing down too much PFADD,</span><br><span class="line"># which is O(N) with the sparse encoding. The value can be raised to</span><br><span class="line"># ~ 10000 when CPU is not a concern, but space is, and the data set is</span><br><span class="line"># composed of many HyperLogLogs with cardinality in the 0 - 15000 range.</span><br><span class="line">hll-sparse-max-bytes 3000</span><br><span class="line"></span><br><span class="line"># Streams macro node max size &#x2F; items. The stream data structure is a radix</span><br><span class="line"># tree of big nodes that encode multiple items inside. Using this configuration</span><br><span class="line"># it is possible to configure how big a single node can be in bytes, and the</span><br><span class="line"># maximum number of items it may contain before switching to a new node when</span><br><span class="line"># appending new stream entries. If any of the following settings are set to</span><br><span class="line"># zero, the limit is ignored, so for instance it is possible to set just a</span><br><span class="line"># max entires limit by setting max-bytes to 0 and max-entries to the desired</span><br><span class="line"># value.</span><br><span class="line">stream-node-max-bytes 4096</span><br><span class="line">stream-node-max-entries 100</span><br><span class="line"></span><br><span class="line"># Active rehashing uses 1 millisecond every 100 milliseconds of CPU time in</span><br><span class="line"># order to help rehashing the main Redis hash table (the one mapping top-level</span><br><span class="line"># keys to values). The hash table implementation Redis uses (see dict.c)</span><br><span class="line"># performs a lazy rehashing: the more operation you run into a hash table</span><br><span class="line"># that is rehashing, the more rehashing &quot;steps&quot; are performed, so if the</span><br><span class="line"># server is idle the rehashing is never complete and some more memory is used</span><br><span class="line"># by the hash table.</span><br><span class="line">#</span><br><span class="line"># The default is to use this millisecond 10 times every second in order to</span><br><span class="line"># actively rehash the main dictionaries, freeing memory when possible.</span><br><span class="line">#</span><br><span class="line"># If unsure:</span><br><span class="line"># use &quot;activerehashing no&quot; if you have hard latency requirements and it is</span><br><span class="line"># not a good thing in your environment that Redis can reply from time to time</span><br><span class="line"># to queries with 2 milliseconds delay.</span><br><span class="line">#</span><br><span class="line"># use &quot;activerehashing yes&quot; if you don&#39;t have such hard requirements but</span><br><span class="line"># want to free memory asap when possible.</span><br><span class="line">activerehashing yes</span><br><span class="line"></span><br><span class="line"># The client output buffer limits can be used to force disconnection of clients</span><br><span class="line"># that are not reading data from the server fast enough for some reason (a</span><br><span class="line"># common reason is that a Pub&#x2F;Sub client can&#39;t consume messages as fast as the</span><br><span class="line"># publisher can produce them).</span><br><span class="line">#</span><br><span class="line"># The limit can be set differently for the three different classes of clients:</span><br><span class="line">#</span><br><span class="line"># normal -&gt; normal clients including MONITOR clients</span><br><span class="line"># replica  -&gt; replica clients</span><br><span class="line"># pubsub -&gt; clients subscribed to at least one pubsub channel or pattern</span><br><span class="line">#</span><br><span class="line"># The syntax of every client-output-buffer-limit directive is the following:</span><br><span class="line">#</span><br><span class="line"># client-output-buffer-limit &lt;class&gt; &lt;hard limit&gt; &lt;soft limit&gt; &lt;soft seconds&gt;</span><br><span class="line">#</span><br><span class="line"># A client is immediately disconnected once the hard limit is reached, or if</span><br><span class="line"># the soft limit is reached and remains reached for the specified number of</span><br><span class="line"># seconds (continuously).</span><br><span class="line"># So for instance if the hard limit is 32 megabytes and the soft limit is</span><br><span class="line"># 16 megabytes &#x2F; 10 seconds, the client will get disconnected immediately</span><br><span class="line"># if the size of the output buffers reach 32 megabytes, but will also get</span><br><span class="line"># disconnected if the client reaches 16 megabytes and continuously overcomes</span><br><span class="line"># the limit for 10 seconds.</span><br><span class="line">#</span><br><span class="line"># By default normal clients are not limited because they don&#39;t receive data</span><br><span class="line"># without asking (in a push way), but just after a request, so only</span><br><span class="line"># asynchronous clients may create a scenario where data is requested faster</span><br><span class="line"># than it can read.</span><br><span class="line">#</span><br><span class="line"># Instead there is a default limit for pubsub and replica clients, since</span><br><span class="line"># subscribers and replicas receive data in a push fashion.</span><br><span class="line">#</span><br><span class="line"># Both the hard or the soft limit can be disabled by setting them to zero.</span><br><span class="line">client-output-buffer-limit normal 0 0 0</span><br><span class="line">client-output-buffer-limit replica 256mb 64mb 60</span><br><span class="line">client-output-buffer-limit pubsub 32mb 8mb 60</span><br><span class="line"></span><br><span class="line"># Client query buffers accumulate new commands. They are limited to a fixed</span><br><span class="line"># amount by default in order to avoid that a protocol desynchronization (for</span><br><span class="line"># instance due to a bug in the client) will lead to unbound memory usage in</span><br><span class="line"># the query buffer. However you can configure it here if you have very special</span><br><span class="line"># needs, such us huge multi&#x2F;exec requests or alike.</span><br><span class="line">#</span><br><span class="line"># client-query-buffer-limit 1gb</span><br><span class="line"></span><br><span class="line"># In the Redis protocol, bulk requests, that are, elements representing single</span><br><span class="line"># strings, are normally limited ot 512 mb. However you can change this limit</span><br><span class="line"># here.</span><br><span class="line">#</span><br><span class="line"># proto-max-bulk-len 512mb</span><br><span class="line"></span><br><span class="line"># Redis calls an internal function to perform many background tasks, like</span><br><span class="line"># closing connections of clients in timeout, purging expired keys that are</span><br><span class="line"># never requested, and so forth.</span><br><span class="line">#</span><br><span class="line"># Not all tasks are performed with the same frequency, but Redis checks for</span><br><span class="line"># tasks to perform according to the specified &quot;hz&quot; value.</span><br><span class="line">#</span><br><span class="line"># By default &quot;hz&quot; is set to 10. Raising the value will use more CPU when</span><br><span class="line"># Redis is idle, but at the same time will make Redis more responsive when</span><br><span class="line"># there are many keys expiring at the same time, and timeouts may be</span><br><span class="line"># handled with more precision.</span><br><span class="line">#</span><br><span class="line"># The range is between 1 and 500, however a value over 100 is usually not</span><br><span class="line"># a good idea. Most users should use the default of 10 and raise this up to</span><br><span class="line"># 100 only in environments where very low latency is required.</span><br><span class="line">hz 10</span><br><span class="line"></span><br><span class="line"># Normally it is useful to have an HZ value which is proportional to the</span><br><span class="line"># number of clients connected. This is useful in order, for instance, to</span><br><span class="line"># avoid too many clients are processed for each background task invocation</span><br><span class="line"># in order to avoid latency spikes.</span><br><span class="line">#</span><br><span class="line"># Since the default HZ value by default is conservatively set to 10, Redis</span><br><span class="line"># offers, and enables by default, the ability to use an adaptive HZ value</span><br><span class="line"># which will temporary raise when there are many connected clients.</span><br><span class="line">#</span><br><span class="line"># When dynamic HZ is enabled, the actual configured HZ will be used as</span><br><span class="line"># as a baseline, but multiples of the configured HZ value will be actually</span><br><span class="line"># used as needed once more clients are connected. In this way an idle</span><br><span class="line"># instance will use very little CPU time while a busy instance will be</span><br><span class="line"># more responsive.</span><br><span class="line">dynamic-hz yes</span><br><span class="line"></span><br><span class="line"># When a child rewrites the AOF file, if the following option is enabled</span><br><span class="line"># the file will be fsync-ed every 32 MB of data generated. This is useful</span><br><span class="line"># in order to commit the file to the disk more incrementally and avoid</span><br><span class="line"># big latency spikes.</span><br><span class="line">aof-rewrite-incremental-fsync yes</span><br><span class="line"></span><br><span class="line"># When redis saves RDB file, if the following option is enabled</span><br><span class="line"># the file will be fsync-ed every 32 MB of data generated. This is useful</span><br><span class="line"># in order to commit the file to the disk more incrementally and avoid</span><br><span class="line"># big latency spikes.</span><br><span class="line">rdb-save-incremental-fsync yes</span><br><span class="line"></span><br><span class="line"># Redis LFU eviction (see maxmemory setting) can be tuned. However it is a good</span><br><span class="line"># idea to start with the default settings and only change them after investigating</span><br><span class="line"># how to improve the performances and how the keys LFU change over time, which</span><br><span class="line"># is possible to inspect via the OBJECT FREQ command.</span><br><span class="line">#</span><br><span class="line"># There are two tunable parameters in the Redis LFU implementation: the</span><br><span class="line"># counter logarithm factor and the counter decay time. It is important to</span><br><span class="line"># understand what the two parameters mean before changing them.</span><br><span class="line">#</span><br><span class="line"># The LFU counter is just 8 bits per key, it&#39;s maximum value is 255, so Redis</span><br><span class="line"># uses a probabilistic increment with logarithmic behavior. Given the value</span><br><span class="line"># of the old counter, when a key is accessed, the counter is incremented in</span><br><span class="line"># this way:</span><br><span class="line">#</span><br><span class="line"># 1. A random number R between 0 and 1 is extracted.</span><br><span class="line"># 2. A probability P is calculated as 1&#x2F;(old_value*lfu_log_factor+1).</span><br><span class="line"># 3. The counter is incremented only if R &lt; P.</span><br><span class="line">#</span><br><span class="line"># The default lfu-log-factor is 10. This is a table of how the frequency</span><br><span class="line"># counter changes with a different number of accesses with different</span><br><span class="line"># logarithmic factors:</span><br><span class="line">#</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line"># | factor | 100 hits   | 1000 hits  | 100K hits  | 1M hits    | 10M hits   |</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line"># | 0      | 104        | 255        | 255        | 255        | 255        |</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line"># | 1      | 18         | 49         | 255        | 255        | 255        |</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line"># | 10     | 10         | 18         | 142        | 255        | 255        |</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line"># | 100    | 8          | 11         | 49         | 143        | 255        |</span><br><span class="line"># +--------+------------+------------+------------+------------+------------+</span><br><span class="line">#</span><br><span class="line"># NOTE: The above table was obtained by running the following commands:</span><br><span class="line">#</span><br><span class="line">#   redis-benchmark -n 1000000 incr foo</span><br><span class="line">#   redis-cli object freq foo</span><br><span class="line">#</span><br><span class="line"># NOTE 2: The counter initial value is 5 in order to give new objects a chance</span><br><span class="line"># to accumulate hits.</span><br><span class="line">#</span><br><span class="line"># The counter decay time is the time, in minutes, that must elapse in order</span><br><span class="line"># for the key counter to be divided by two (or decremented if it has a value</span><br><span class="line"># less &lt;&#x3D; 10).</span><br><span class="line">#</span><br><span class="line"># The default value for the lfu-decay-time is 1. A Special value of 0 means to</span><br><span class="line"># decay the counter every time it happens to be scanned.</span><br><span class="line">#</span><br><span class="line"># lfu-log-factor 10</span><br><span class="line"># lfu-decay-time 1</span><br><span class="line"></span><br><span class="line">########################### ACTIVE DEFRAGMENTATION #######################</span><br><span class="line">#</span><br><span class="line"># WARNING THIS FEATURE IS EXPERIMENTAL. However it was stress tested</span><br><span class="line"># even in production and manually tested by multiple engineers for some</span><br><span class="line"># time.</span><br><span class="line">#</span><br><span class="line"># What is active defragmentation?</span><br><span class="line"># -------------------------------</span><br><span class="line">#</span><br><span class="line"># Active (online) defragmentation allows a Redis server to compact the</span><br><span class="line"># spaces left between small allocations and deallocations of data in memory,</span><br><span class="line"># thus allowing to reclaim back memory.</span><br><span class="line">#</span><br><span class="line"># Fragmentation is a natural process that happens with every allocator (but</span><br><span class="line"># less so with Jemalloc, fortunately) and certain workloads. Normally a server</span><br><span class="line"># restart is needed in order to lower the fragmentation, or at least to flush</span><br><span class="line"># away all the data and create it again. However thanks to this feature</span><br><span class="line"># implemented by Oran Agra for Redis 4.0 this process can happen at runtime</span><br><span class="line"># in an &quot;hot&quot; way, while the server is running.</span><br><span class="line">#</span><br><span class="line"># Basically when the fragmentation is over a certain level (see the</span><br><span class="line"># configuration options below) Redis will start to create new copies of the</span><br><span class="line"># values in contiguous memory regions by exploiting certain specific Jemalloc</span><br><span class="line"># features (in order to understand if an allocation is causing fragmentation</span><br><span class="line"># and to allocate it in a better place), and at the same time, will release the</span><br><span class="line"># old copies of the data. This process, repeated incrementally for all the keys</span><br><span class="line"># will cause the fragmentation to drop back to normal values.</span><br><span class="line">#</span><br><span class="line"># Important things to understand:</span><br><span class="line">#</span><br><span class="line"># 1. This feature is disabled by default, and only works if you compiled Redis</span><br><span class="line">#    to use the copy of Jemalloc we ship with the source code of Redis.</span><br><span class="line">#    This is the default with Linux builds.</span><br><span class="line">#</span><br><span class="line"># 2. You never need to enable this feature if you don&#39;t have fragmentation</span><br><span class="line">#    issues.</span><br><span class="line">#</span><br><span class="line"># 3. Once you experience fragmentation, you can enable this feature when</span><br><span class="line">#    needed with the command &quot;CONFIG SET activedefrag yes&quot;.</span><br><span class="line">#</span><br><span class="line"># The configuration parameters are able to fine tune the behavior of the</span><br><span class="line"># defragmentation process. If you are not sure about what they mean it is</span><br><span class="line"># a good idea to leave the defaults untouched.</span><br><span class="line"></span><br><span class="line"># Enabled active defragmentation</span><br><span class="line"># activedefrag yes</span><br><span class="line"></span><br><span class="line"># Minimum amount of fragmentation waste to start active defrag</span><br><span class="line"># active-defrag-ignore-bytes 100mb</span><br><span class="line"></span><br><span class="line"># Minimum percentage of fragmentation to start active defrag</span><br><span class="line"># active-defrag-threshold-lower 10</span><br><span class="line"></span><br><span class="line"># Maximum percentage of fragmentation at which we use maximum effort</span><br><span class="line"># active-defrag-threshold-upper 100</span><br><span class="line"></span><br><span class="line"># Minimal effort for defrag in CPU percentage</span><br><span class="line"># active-defrag-cycle-min 5</span><br><span class="line"></span><br><span class="line"># Maximal effort for defrag in CPU percentage</span><br><span class="line"># active-defrag-cycle-max 75</span><br><span class="line"></span><br><span class="line"># Maximum number of set&#x2F;hash&#x2F;zset&#x2F;list fields that will be processed from</span><br><span class="line"># the main dictionary scan</span><br><span class="line"># active-defrag-max-scan-fields 1000</span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>DevOps</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
        <tag>Docker</tag>
        <tag>redis</tag>
      </tags>
  </entry>
</search>
